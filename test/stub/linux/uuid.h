/* Stub for the shim gate: only the one name hammer2_compat.h calls. */
#ifndef _STUB_LINUX_UUID_H
#define _STUB_LINUX_UUID_H

static inline void generate_random_uuid(unsigned char uuid[16]) { (void)uuid; }

#endif
