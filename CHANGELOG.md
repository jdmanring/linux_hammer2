Changelog
=========

Every point release this port has made, newest last, each pinned to the
commit that delivered it and to the gate that verified it. A row is added
when a deliverable is verified, not when work starts.

Versions are milestones, not calendar releases. The first two numbers say
how far along the ladder in `doc/README.roadmap.md` the driver is; the
third is the point release and increments inside the current milestone.
Until 1.0 the number says nothing about stability. Nothing here is
released: no tag exists and the driver does not mount.

The numbers before 0.1.11 are assigned retroactively to the state at each
commit. Two rewrites of this repository's history have happened since,
both recorded in `doc/history/`, so a hash here is the current one and
`doc/history/2026-08-25-rewrite-commit-map.txt` maps the older set.

`script/test-history.sh` reads this file: every row must pin a hash that
resolves, no two rows may share a version, and the subject of the commit
is printed beside the row so a wrong pin is visible rather than merely
resolvable. It also names any commit touching `src/` or `script/` that
has no row yet, which is a prompt and not a failure.

| version | date | delivered | verified by |
|---|---|---|---|
| 0.1.0 | 2026-08-25 | initial import: OS shim, DIO layer on the block device page cache, format, ioctl and in-memory headers, vendored `sys/tree.h` and `sys/queue.h` (`8c84941`) | `script/test-shim.sh`, `script/test-syntax.sh` |
| 0.1.1 | 2026-08-25 | style gate's sort made locale-independent after a first CI run failed with every count identical and four lines reordered (`5269dd0`) | `script/test-checkpatch.sh` byte-comparing its baseline |
| 0.1.2 | 2026-08-25 | version floor corrected from memory to measurement, 6.15 by `BLK_MAX_BLOCK_SIZE`, each symbol dated at its tag (`bb9f2df`) | the table in `README.status.md` |
| 0.1.3 | 2026-08-25 | the folio ceiling's design recorded: a mount-time capability check by name, `mapping_max_folio_size_supported()`, with the build-time THP assert kept as the bootstrap instrument until a mount path exists (`0c80e3e`, documentation only; the check itself is 0.3 criterion 3) | `README.porting.md`, The DIO layer |
| 0.1.4 | 2026-08-25 | vendored headers stopped redefining the kernel's `LIST_HEAD` and `RB_ROOT`, found by adding gcc and a W=1 warning set beside clang (`8877e3f`) | `script/test-syntax.sh`, now 7 checks across two compilers |
| 0.1.5 | 2026-08-25 | the 64 KiB inventory: thirteen sites, seven the on-disk format and six the folio decision, in `IO_MODEL.md` (`3ad0dd4`) | the inventory itself, each site cited to a line |
| 0.1.6 | 2026-08-25 | this table got a reader, the module's absent `MODULE_LICENSE` was recorded with what decides its value (`644403b`), and the style gate stopped writing a baseline and exiting 0 when it found none (`af95fb4`) | `script/test-history.sh`, falsified on an unresolvable hash and on a table whose rows stop matching; the checkpatch branch exercised through a stub, all three cases read one invocation at a time |
| 0.1.7 | 2026-08-25 | a gate over the three hand-maintained lists that claim to cover `src/sys/fs/hammer2/`: the origin table, the Makefile's `hammer2-y`, and the filenames `script/test-syntax.sh` names one by one (`e8f5fc8`) | `script/test-inventory.sh`, falsified with a planted `.c` that produces one finding per list; population asserted |
| 0.1.8 | 2026-08-25 | the 0.1.x rows audited claim by claim against the tree rather than against this table, and the one overclaim found corrected: `README.status.md` said "the first eight of those are expected to carry", which put `inode` and `subr` in the carried set on no cited authority and pre-decided what 0.2 routes to the provenance CSV. The measured set is the six of `H1_ESTIMATE.md` (`94f48da`). A gate now reads every `file:line` citation in `doc/` against the line it names | `script/test-citations.sh`, falsified one invocation at a time on a one-line drift, a citation to a file that does not exist, a line past end of file, and an emptied population, which returns COULD-NOT-RUN rather than passing; what it cannot catch is named in its own header |
| 0.1.9 | 2026-08-25 | the citation gate's anchor strengthened, because the count it printed overstated what it checked: it took the first backticked identifier in a row's prose, so `dio->psize` anchored on `dio` and `struct hammer2_dev` would have anchored on `struct`, either of which matches almost any line in the cited file. The ten passes were real only because they had been read by hand (`3d4cc49`) | the whole backticked token is the anchor, with a trailing `()` stripped; a token that is not a plain identifier is matched in full and reported as literal-anchored rather than counted with the symbol-anchored ones, so pass strength is visible instead of averaged. The repair's own first version left the previous row's symbol in place on a `case` with no matching branch, which was caught by rerunning rather than by reading |
| 0.1.10 | 2026-08-25 | the reader-facing documents audited against the tree: three gates that no document or CI step named, a uniform control claim that was false for four of six gates, an errno deviation count that had never matched its baseline, two drifted line counts, a CI comment naming the wrong kernel floor, and nine unrecorded point releases (`5d98fd2`) | `script/test-inventory.sh`, extended to compare the origin table's line count against the file and falsified three ways: perturbing the doc fails, perturbing the file fails, and a non-numeric count column is left alone rather than guessed at |
| 0.1.11 | 2026-08-25 | the style gate stopped comparing a byte-exact baseline against a moving checker. CI fetched `checkpatch.pl` from `torvalds/master`, so the same unchanged tree scored 579 hits under master and 582 under v6.15: a check added upstream turns the badge red for a reason that is not this code, and a check dropped upstream shrinks the count and reads as an improvement. CI now pins v6.15, the module's own declared kernel floor (`15c08f7`), and the comparison logic that makes the pin load-bearing is in `16a9aef`, which also carried the removal of this tree's references to a private upstream | the version is recorded in the first line of `doc/checkpatch-baseline.txt`, where it cannot drift from the numbers it qualifies, and comment lines are stripped before the diff. Falsified three ways: the pinned version passes, master fails with a message naming the expected version first, an absent baseline is still exit 2 |
| 0.1.12 | 2026-08-25 | the inventory gate's own comment corrected on two counts: it named the line count as the second column where the leading pipe makes it the third pipe-delimited field, which would have sent the next rewrite to the wrong field, and it justified `sed` over `awk -F'|'` by saying the behaviour could not be tested here. It could: POSIX uses a single-character `FS` literally and `gawk --posix` confirms it in one command (`fa22abb`) | the extraction was already correct, so the gate's own output does not move; what changed is that a reader is no longer told a measurement was unavailable when it was one command away |
| 0.1.13 | 2026-08-25 | the style gate learned to test the version it had only ever printed. The baseline is a claim about a checker version, and on a workstation whose build tree is Artix 7.1 one category of twenty-nine differs, 18 against 15, which exited 1 with the same status as a real regression on a machine where nobody could turn it green (`50ae15a`) | the version comes from the Makefile two levels above `checkpatch.pl`, `CHECKPATCH_REF` overrides for a copy out of its tree, and a proven mismatch is COULD-NOT-RUN. An unestablished version is disqualifying only on a diff, so a regression cannot be laundered into could-not-run |
| 0.1.14 | 2026-08-26 | the folio length check stopped being an assertion the default build compiles out. `hammer2_io_data()` hands the core a `folio_address()` pointer it reads `psize` bytes through, and both sites guarded that with `KKASSERT`, which is a no-op unless `HAMMER2_INVARIANTS` is set. One helper both callers route through now checks the length, releases the folio and fails the I/O with a positive `EIO`; `pr_fmt` was missing too, so nothing this file printed carried the module name (`268b69f`) | `script/test-syntax.sh` under both compilers with the knob in both positions; FreeBSD has no counterpart to track, because `getblk()` is told the size it must return |
| 0.1.15 | 2026-08-26 | the panic policy recorded as a port decision, and one style disposition corrected against a measurement. `checkpatch.pl` demotes AVOID_BUG to check under `--file`, the mode the gate runs, so eight `BUG_ON` and four `panic()` sites produce no hits and the baseline has no row for them; and the `__inline` row claimed its nine hits were in the vendored `sys/tree.h` and `sys/queue.h`, which the gate has never scanned at all (`a869863`, `9e0af9e`) | counted per file: `hammer2.h` 6, `hammer2_io.c` 2, `hammer2_rb.h` 1. The decision is split by half, the carried core keeping its assertions and new OS-half code getting `WARN_ONCE` plus recovery, with a `DEFER` on `hpanic` naming the VFS layer as the trigger |
| 0.1.16 | 2026-08-26 | `test/` became the inventory gate's second population, after two vector files were found tracked, named by no gate and mentioned in no document since the initial import. Both were also wrong in the way an unrun test always is: `crc32c-vectors.c` accepted either Castagnoli or IEEE, and `xxh64-vectors.c` asserted nothing on two of three cases and seeded the third with xxHash's golden-ratio prime where HAMMER2 seeds with `0x4d617474446c6c6e` (`c78c19c`) | every file under `test/` is now named by a gate or listed as staged in `README.testing.md`, falsified both ways with a planted top-level orphan caught and a planted stub header correctly passing under the `-I` the shim gate gives it. The four xxh64 vectors were measured against xxhsum 0.8.3 and libxxhash 0.8.3, compiled and run green against the system xxHash, and falsified by planting a wrong digest |
| 0.1.17 | 2026-08-26 | the history gate reads version numbers as well as hashes. Every check it had passed on a table carrying two rows numbered 0.1.13, written in one edit while adding a batch of rows: a shared number makes the older row unreachable by the name a reader looks it up by, and this table is the only place a version is bound to a commit (`d8094fd`) | duplicates are counted into the same `bad` total as an unresolvable hash, and falsified by renumbering one row onto its neighbour, which exits 1, and by restoring it, which exits 0 |
| 0.1.18 | 2026-08-26 | `hpanic` printed no module name either, which 0.1.14's `pr_fmt` did not fix and its message half-claimed. `panic()` is not a `pr_*` macro and takes its format verbatim, so the prefix reached `hprintf` and nothing else; measured by preprocessing against the real headers rather than by reading the macro (`639565f`) | `hpanic` carries `KBUILD_MODNAME` itself, one marked token of divergence from the BSD ports. The shim gate never expanded the macro at all, so it now does, behind a constant false since it is noreturn, with the stub supplying what only kbuild defines. Falsified by removing that define, which fails on the `hpanic` line |
| 0.1.19 | 2026-08-26 | 0.1.16 called both vector files run by nothing. They are compiled by Saxum's `scripts/test-hammer2-checkalg.sh`, which reaches this tree through `LINUX_HAMMER2`; the sweep behind that row searched this repository only, and an absence measured in one tree is not an absence. Worse, the rewrite lowercased the constant that gate's negative control seds on, so its control compared a file to itself and it reported for an hour that it was comparing nothing (`594efaf`) | the constants are uppercase again and `-DXXH_VECTORS_CONTROL` corrupts the first expected digest at compile time, so no formatting change can silence that control again. Verified by running the consumer's gate rather than reasoning about it: red before, green after, both halves, the CRC half confirming `iscsi_crc32` is Castagnoli against the real `icrc32.c` |
| 0.1.20 | 2026-08-26 | the syntax gate compiled against whatever kernel tree was present and called it a pass. The kernel of record is the latest release, every document said 7.2, and the gate has always printed the kernel it used in its own header line: two strings nobody ever compared, so every green run on the development host was 7.1.9 read as 7.2 (`96532e6`) | a tree that is not the kernel of record is COULD-NOT-RUN, mirroring the checkpatch version check exactly. Measured under the `H2_KERNEL_REF` override rather than assumed: 7.1.9-artix1-2 and 6.18.46-1-lts both pass 7 checks on both compilers. No 7.2 tree exists here - Artix ships 7.1.9 and Saxum's cachyos 7.2.0 `dev` output is unrealized - so the port is UNVERIFIED against its own kernel of record and prints that instead of green |
| 0.1.21 | 2026-08-26 | the contract with the out-of-tree consumer lived in that consumer's `sed`, so a legitimate edit here broke a negative control there and this tree stayed green. A seventh gate freezes this side of it: the `-DXXH_VECTORS_CONTROL` hook, the uppercase constants, and the `printf` that writes `Castagnoli ... match`. It reaches across nothing (`f26170b`) | case-sensitive by construction, with a lowercased copy required to fail on every run; the exit status asserted in both directions; all three elements falsified individually. Writing it reproduced the class it catches, since the `crc32c` pattern first matched the comment quoting the wording and stayed green with the `printf` deleted. The gate count is derived now too, five documents having said "six" until the seventh existed |
| 0.1.22 | 2026-08-26 | `checkpatch.pl` was pinned at v6.15 to match the module's floor, where the rule is to compile against the latest release: the checker should be the kernel of record's, and the floor is a different claim (`cde5e39`) | re-pinned to v7.2 and measured rather than adopted. Exactly one category moves, 18 hits to 15, total 586 to 583, every other line byte-identical - which is also the signature a 7.1 distribution checker produced against the old baseline, so the version check had been calling it a mismatch correctly all along. The AVOID_BUG demotion was re-read at v7.2 line 4915 rather than assumed to survive |
| 0.1.23 | 2026-08-26 | a citation failure named where the anchor was not and never where it is, so repairing one meant re-deriving a line by hand, which is where a confident wrong number gets written: the spec repository had cited `BLK_MAX_BLOCK_SIZE` at a line matching neither the tag it named nor the one before it (`91af648`) | the failure now prints the anchor's real lines, and widens to the tree when it is not in the cited file at all, since line drift is survivable and file drift is not. Falsified on both: a one-line drift reports the real lines, a citation moved to another file reports which files hold the anchor. `IO_MODEL.md`'s `blkdev.h` citation, the only one pointing outside this tree and so the only one no gate can check, is by expression now |
| 0.1.24 | 2026-08-26 | the citation gate reads table rows, so a citation in prose - or one naming a kernel header or another port's tree - was matched by nothing and never mentioned, sitting among checked rows and inheriting their credibility. Twenty tokens in `doc/`, fifteen of them read (`540b47b`) | the uncovered count prints beside the covered one and names each, floored because zero uncovered is also what a stopped pattern prints; falsified by breaking the pattern, which reports 0, names the floor and exits 1. Naming them emptied the class: two cited a sibling port by a bare basename that collides with a file of ours, two pinned kernel lines read from a 7.1.9 tree while the kernel of record is 7.2, one in-tree prose line nothing checked. All by expression now, each verified against the local headers first |
| 0.1.25 | 2026-08-26 | `IO_MODEL.md` had opened by naming 7.2.0-cachyos the kernel of record, calling it the newest realized `linux-*-dev` output in the store, and telling the reader to re-read rather than cite the line. Nobody did: no such output is realized here, and the nix fallback it named fires only when `/lib/modules/$(uname -r)/build` is absent, which it never was, so that path had not been taken once (`1acd229`) | the same defect 0.1.20 found from the gate's end, written in a document a day earlier. Also re-prices reaching 7.2: the `dev` output is prebuilt and signed in the CachyOS cache at 687 MB with a deriver matching the `.drv` already in this store, read from the narinfo rather than accepted, so it is a download and its closure and not the LTO kernel build recorded an hour before |
| 0.1.26 | 2026-08-26 | the syntax gate named the kernel it used and never the branch that found it, and one of those branches had never fired: `IO_MODEL.md` described the nix-store fallback as the source of the kernel of record while `/lib/modules/$(uname -r)/build` was present on every run, so document and script agreed in wording, disagreed in behaviour, and neither could detect the other (`a9a8d6b`) | the header line names the resolution on every run, including runs delegated from another repository. All three observed rather than reasoned about, the nix branch by pointing `KDIR` at a path that does not exist: first time it has ever run here, and it returns COULD-NOT-RUN naming itself rather than proceeding |
| 0.1.27 | 2026-08-26 | the same unnamed-branch class swept through the other gates. `CHECKPATCH_REF` is a human assertion and the Makefile above `checkpatch.pl` is derived, and the output printed both as "v7.2": every run of that gate here today was told rather than measuring, because the pinned copy lives outside a kernel tree. The syntax gate's dialect flag had the same shape, empty meaning both absent `.config` and not-needed (`c43a7b1`) | origin printed beside every such value, and pinned to nothing: which branch is right depends on the machine, so constraining one would break the case its fallback exists for. Both branches exercised - the derived path resolves the local 7.1 checker and returns COULD-NOT-RUN against the v7.2 baseline |
| 0.1.28 | 2026-08-26 | `H2_KERNEL_REF` is a loosened threshold and it hid in the summary line: an overridden run printed `syntax: 7 check(s), 0 failed`, byte-identical to a reading against the kernel of record, and with no 7.2 tree on the development host every such line reported that day came from an overridden run (`59afa76`) | the summary names the version actually read and states the run is not evidence about the target kernel; a real reading names the kernel of record instead. Both branches exercised, the second by pinning `KERNEL_REF` to the installed version. The class is Saxum's, from a disk floor somebody could drop to get past a gate: a loosened threshold must not produce the output an unloosened run produces |
| 0.1.29 | 2026-08-26 | the style gate's checker version came from a `CHECKPATCH_REF` assertion or from the Makefile above the checker, neither of which survives the file being copied out of its tree - the condition every run here meets, since the pinned copy lives in `/tmp`. Falsifying the repair found the hole that mattered: pointed at the v6.15 checker with `CHECKPATCH_REF=v7.2` the gate exited 1, reporting a real style regression against this code from a checker that never produced the baseline (`bdad5a1`) | the baseline's second line records the checker's `sha256`, the identity the file answers for itself; `checkpatch.pl`'s own `$V` is inert at 0.32 across v6.15, v7.2 and the local 7.1 copy. A content mismatch is COULD-NOT-RUN whatever names the checker carries, provenance prints on the diff branch too, and a genuine regression under the matching checker still exits 1 |
| 0.1.30 | 2026-08-26 | the two prints that keep a loosened run from reading like a real one, both added the same day to fix exactly the class where unread output gets trusted, were themselves read by nothing (`2010204`) | `--selftest` on the syntax and style gates, run by CI. The syntax one drives an overridden run and requires the warning; the style one copies the checker and appends a comment so content differs while behaviour does not, covering the branch where an assertion used to launder a wrong checker. It failed on its first run and the defect was the fixture's: the warning wraps, so a line-at-a-time matcher called it missing - the same class the inventory gate was fixed for that morning, in a matcher written for wrapped prose |
| 0.1.31 | 2026-08-26 | the syntax gate's immunity to a version string that is correct about A different subject was luck of construction rather than design. `linux-api-headers 7.2-1` is installed, so `/usr/include/linux/version.h` reads MAJOR 7 PATCHLEVEL 2 with no build tree beside it; the gate reads a `Makefile` and so cannot be fooled, but nobody had chosen that (`c56fc3b`) | a third selftest check: a directory holding only a `version.h` claiming 7.2 must be COULD-NOT-RUN. Falsified with the plausible later improvement, a `version.h` fallback when the `Makefile` is absent, under which the gate accepts the fake and prints `7 check(s), 5 failed against the kernel of record (7.2)`. Swept this tree for the exposure: 21 files, no script reads a kernel version by any route, every `uname -r` locating a build tree rather than determining a version |
| 0.1.32 | 2026-08-26 | CI typed the list of gates that check this tree and had already fallen behind - the vectors contract gate ran nowhere in CI while the agent instructions file disclosed that instead of fixing it. And the vectors gate's own control ran a separate inline comparison from the one it guards, so a `-i` on the real check would have left it green (`42dbcdb`, `0aeaf05`) | CI enumerates `script/test-*.sh` and derives the selftest list by grepping for the flag, with exit 2 a recorded skip and a failure outranking it; verified by running the same loop locally and parsing the workflow with `yq` rather than by eye. The control routes through the same `matches()` now, falsified by making it case-insensitive. The second came from reading Saxum's errata, where a falsification exited 1 without reaching the gate it was written for |
| 0.1.33 | 2026-08-26 | two checks had never been observed failing, so nothing showed they tested anything: the HAMMER2 seed constant, which shared a code path with a falsified one but had not itself been tried, and the checkpatch selftest (`818b6da`) | both run against their defect now and both fail naming the right check, the second's output showing the gate say "asserted via CHECKPATCH_REF" with no mismatch warning, which is the laundering state it exists to catch. Recorded as dated rather than as a property, since it rots the moment a check is added, and the one weak direction is named rather than counted |
| 0.1.34 | 2026-08-26 | 0.1.33 recorded that every check had been observed failing, which is a claim about a population that grows: false the moment a check is added rather than eventually, with nothing to notice (`91f16a0`) | replaced by a dated table of what was falsified and how, which cannot rot because it never claimed completeness, plus the obligation that a new check arrives with the run that showed it failing. The direction that cannot be falsified on the development host is absent from the table rather than listed with a caveat, since a caveat inside a list still reads as coverage |
| 0.1.35 | 2026-08-26 | two decisions lived only in a peer conversation: that no gate derives the falsification table, and why. A decision not to build something is invisible to the next reader unless it is in the tree they grep (`aa69539`) | both in `README.testing.md` now, with the sharper form of 0.1.34's reasoning beside them: a date repairs aging and never falsification, since the date stays true while the sentence stops being, so nothing about a dated completeness claim ever looks stale |
| 0.1.36 | 2026-08-26 | the port type-checks against its kernel OF record for the first time. The chaotic 7.2.0-cachyos `dev` output was substituted into the store at the maintainer's direction and the gate reads `7 check(s), 0 failed against the kernel of record (7.2)`, both compilers, both controls firing, no override. Every syntax result recorded before this was 7.1.9 read as 7.2 (`1bd8fad`) | the first run against the real tree failed and the guard was what was wrong: a nix dev output's `build/Makefile` is a stub that includes the real Makefile from the `source` directory beside it, so the version was not in the file being read, and a legitimate tree with `scripts/`, `arch/` and `Module.symvers` was rejected. It follows the `include` line the stub names now, derived from the artifact rather than from a convention; `linux-api-headers` still fails, having no Makefile to follow, and the UAPI selftest check still passes |
| 0.1.37 | 2026-08-26 | the gate needed `KDIR` typed to reach the kernel it targets, so CI and the delegator that runs it from another repository - both of which invoke it with no arguments - would have kept reporting COULD-NOT-RUN with the right tree sitting in the store. And the compiler was never checked against anything (`32d2086`, `7a35b93`) | the first candidate whose own Makefile reports the wanted version wins, running kernel before store, with an explicit `KDIR` still overriding. The compiler is compared against the tree's own `CONFIG_CC_VERSION_TEXT` and printed either way: `clang version 22.1.8` matches the kernel of record byte for byte, and reads `NOT the tree's own` against the 6.18 and 7.1.9 trees. Recorded with it: 7.2.0-cachyos is not mainline 7.2, `source/` is pruned to 81 `.c` files with no `drivers`, and the tree's own `checkpatch.pl` differs from mainline's by hash while producing the identical 583 |
| 0.1.38 | 2026-08-26 | the style gate's default could not find the kernel of record's own checker either, the same defect one file over: it looked only at the host's build tree while a `checkpatch.pl` sat in the store under `source/scripts` (`358f749`) | three tiers, strongest first - the file the baseline was produced with by `sha256`, then any checker whose own tree reports the baseline's version, then whatever exists so a refusal can name it. every gate then in the tree ran green with no environment variables set, against 7.2.0-cachyos, and Saxum's delegator reports 7 ran / 0 could not run where it read 5 and 2 an hour earlier |
| 0.1.39 | 2026-08-26 | two more gates could not say which question they had answered: `test-shim.sh` and the vectors contract use `cc`, which is gcc here, while a reviewer reaching for clang gets a different opinion by construction and the output named neither (`7c8233e`) | every gate that uses a toolchain names it and every gate that resolves a tree names how. The shape is the loosened threshold with the polarity flipped - there the override was the only working path, here the default was the only broken one - and in both the summary line could not say which had happened |
| 0.1.40 | 2026-08-26 | 0.1.39's own documentation failed the inventory gate: a partial count written in the shape reserved for a claim about the total, which is the false positive named in that gate's design and then written anyway. It was committed red and caught by a later sweep rather than before the commit (`4b46501`) | the gate was right and the prose names them now. Two further known classes on the way out: the first repair matched nothing because the phrase wrapped, so the edit did nothing and the commit read as a fix while being empty, and the second reintroduced the literal phrase inside the sentence explaining the failure, a check reading the file rather than the intent |
| 0.1.41 | 2026-08-26 | the selftests re-invoked their own gate with `sh "$0"`, which works where `/bin/sh` is bash and is a syntax error under dash, so they passed here and failed on the runner, after 59 commits pushed without a CI result being read (`377783d`) | `bash "$0"`, matching the shebang both gates declare. Two more found while fixing it: the unoverridden direction was asserted with `[ -n "$(... grep -c ...)" ]`, always true since `grep -c` prints a number, so it had asserted nothing since the hour it was written; it requires a compile against the kernel of record now, and prints a NOT-EXERCISED note where that tree is absent rather than failing, since failing there makes an environment difference a red gate. The `#!/bin/sh` gates were swept for bashisms: none |
| 0.1.42 | 2026-08-26 | CI's own pins were behind: `actions/checkout` at v4, targeting Node 20 and being forced onto Node 24 with a deprecation notice every run where v7.0.1 is current, and no xxHash on the runner, so the vectors gate's behavioural half - the exit status in both directions, the half that catches a wrong expected value - had never run there (`e3234d7`) | both fixed and CI green, with exactly one honest skip remaining: `test-syntax.sh`, because `ubuntu-latest` ships headers years behind the kernel of record. The pin rule reaches the actions as much as the kernel and the style checker |
| 0.1.43 | 2026-08-26 | a gate result was quoted without the revision it ran against, and this tree is a live checkout another repository reads while work is being committed to it: Saxum's delegator saw 6 gates forty seconds before seeing 7, having walked the tree mid-commit, which is indistinguishable from broken and teaches re-running until green (`a3da442`) | the recorded result carries its revision now. Recorded with it: pointing the gates at a compiler that does not exist makes three of them decline and name what was missing, while the absent-`perl` path is listed UNVERIFIED, because emptying `PATH` breaks the shell before the gate runs and perl shares a directory with everything else the gate needs - the probe measured nothing, and a count of failures among nothing is a faked pass |
| 0.1.44 | 2026-08-26 | an eighth gate parses the `#!/bin/sh` gates with shells that are not bash, closing the class that turned CI red: every gate here is normally run by bash, so a bash-only construct in such a script breaks only when something honours the shebang. those four had been called clean from a grep, and `command -v` was read as proof that no POSIX shell existed here; PATH is not the machine, and dash and busybox were already in the store (`32d5505`) | it prints its own reach every run, since a clean parse is worth far less than it looks: dash rejects process substitution, arrays and `function`; ash rejects only arrays; both accept `[[ ]]`, `declare`, `local` and `+=` as words. Falsified three ways - a planted process substitution fails under dash by file and line, an array fails under both, and no shell reachable is COULD-NOT-RUN. Registering it made the inventory gate demand five documents be corrected |
| 0.1.45 | 2026-08-26 | the new gate asserted its own reach from a hardcoded table, which is a claim nothing checks sitting where a reader decides whether a clean run means anything - and it was wrong twice, at one construct then at three, where there are four (`d18d7ce`) | the reach is measured from the probe set every invocation and printed as observed. What is asserted are two properties of a working checker that hold whatever the reach is: each shell must reject at least one probe, or it is inert and a clean result means nothing; and a plain POSIX script must be accepted, or a clean result is unreachable rather than earned. Falsified both ways. An under-claim is still a false claim and is the one nobody re-checks, since a modest statement about one's own instrument reads as rigour |
| 0.1.46 | 2026-08-26 | the absent-`perl` path was recorded UNVERIFIED because emptying `PATH` breaks the shell and perl shares a directory with everything the gate needs. Both true, conclusion wrong: a nix machine keeps each tool in its own store path, so a `PATH` without perl is assembled rather than subtracted (`2e95a2f`) | built from store coreutils, sed, grep, diff, awk and bash, the gate exits 2 naming `no perl`. "I cannot check this" is itself a claim about an instrument and decays like any other; the honest shape is UNVERIFIED BY this route with the route named, since "perl cannot be hidden" carries no trace of "by directory, on a PATH-shaped machine". Same error as the Saxum tree's "no POSIX shell here" from `command -v`, and it survived the same way: a concession reads as rigour, so nobody audits it |
| 0.1.47 | 2026-08-26 | twelve COULD-NOT-RUN branches across the eight gates had never been driven. An error path nobody has driven is an untested branch wearing the costume of a safety net: it reads as defensive prose rather than as code, so it is the last thing anyone exercises (`4fb72fc`) | all twelve driven in a scratch copy by removing the input each names, all exit 2 naming what was missing, and two were defective - `test-shim.sh` was the only gate omitting the `COULD-NOT-RUN` prefix, invisible to anything scanning output rather than status, and `test-posix.sh` could not reach its own no-shell branch at all, since the store lookup finds a shell on any machine that has one |
| 0.1.48 | 2026-08-26 | a document can state the right count and enumerate the wrong list, and those are different claims about one population: the agent instructions file, since untracked, said eight gates and listed seven, so the count check passed while the list a future reader runs was short by the newest gate - in the first file an agent working here reads (`5a56b3b`) | the inventory gate requires every gate to be named in the three documents printing runnable command lists; documents that merely mention one are deliberately excluded, since requiring all eight there would be wrong. Falsified by removing a name from `README.md` |
| 0.2.1 | 2026-08-26 | the lock recursion had been recorded as the port's remaining lock gap since 0.1 and left for "when those files land"; `hammer2_chain.c` is one of those files, so leaving it further would have been handing back a measured, reversible choice inside work already scoped (`ad572dc`) | decided the way the NetBSD port decided it: no recursive lock, because a Linux `rw_semaphore` deadlocks against its own holder exactly as a NetBSD `krwlock` does. `hammer2_mtx_init_recurse()` is a plain init and the one recursing path, `hammer2_chain_lookup()` on a DIRECTDATA inode, is closed by never setting the flag - which costs a data block per tiny file and no correctness. That half lands with `hammer2_inode.c`, where the flag is set; until then nothing here recurses. `hammer2_chain.c` carries byte-for-byte at 4929 lines with no core edit |
| 0.2.2 | 2026-08-26 | the inventory gate read the origin row with an unanchored `grep` and `head -1`, so it took whichever row mentioned the file first: `hammer2_mount.h`'s, whose note says "hammer2_chain.c includes it", and reported a 4929-line file as a 58-line one (`ad572dc`) | matched on the first column. A wrong row is worse than a missing one, because it compares a real number against a real number and so reads as a finding about the file rather than about the matcher |
| 0.2.3 | 2026-08-26 | 0.2's first exit criterion - no file under `src/` without a recorded origin, commit and licence - had been prose since the roadmap was drafted, holding only as long as whoever added a file remembered it (`afa2244`, `e3e54fe`, `9371eaa`) | `doc/provenance.csv` and `script/test-provenance.sh`. Two of its three checks read both sides out of this tree; the third re-runs `cmp` against the origin clone, since a row saying "identical" is a claim about another repository. With no clone present it exits 2 rather than passing on a table that only agrees with itself - and the selftest does the same, having no teeth to test either. CI now clones the origin at the commit the CSV names, so that check runs on every push instead of on one workstation |
| 0.2.4 | 2026-08-26 | every log line five carried files printed was anonymous, and some were cut in two. Linux takes the module name from a per-`.c` `pr_fmt` and supplies an empty one where it finds none, so a missing define is valid C that prints a bare message forever; and `printf` mapped to `pr_info`, which closes a record per call, splitting the single lines the core builds out of several calls and dropping the second half's prefix (`210cb9c`) | the name moved into `hprintf`, this port's own macro, which reaches the carried files a header cannot; `printf` maps to `pr_cont`, correct at both kinds of call site where `pr_info` is correct at one. Neither fact is visible to a compiler or to a diff, so `test-shim.sh` reads both out of the preprocessor's expansion, and both checks were driven negatively. checkpatch objects to `pr_cont` by name; the row is in the baseline with a `DEFER` naming its upgrade |
| 0.2.5 | 2026-08-26 | six stale claims across the three documents an agent or a contributor reads first, and prose that credited work to "the specification session" and "the Saxum implementation session" in a tree meant to be read by the maintainers it is carried from (`a84c27f`) | the gate count was deleted rather than corrected, since the row it sat in says never to write one down and nine goes stale on the same two-day clock that made six stale. Every fact the session attributions carried is kept and re-attributed to the tree or the gate |
| 0.2.6 | 2026-08-26 | 0.2.5 audited this file by searching it and reading three sections, which found its prose defects and none of its state: the version line still read 0.1.x above four 0.2 rows, the milestone table still called 0.2 next, two work items were done and unmarked, and the F1 row still said "exists" directly above the section retracting that word (`10cf941`) | read end to end. Two exit criteria named authorities that do not exist and were rewritten against the provenance CSV and the measured `XXX` population, recorded in the revisions section as that section requires |
| 0.2.7 | 2026-08-26 | the agent instructions and the symbol index were tracked, and had been public since the repository was created: tooling for whoever is working on the tree, not part of what a maintainer carrying this port would take (`81d38f9`, `6a1451e`) | every fact `CLAUDE.md` held checked one by one against `README.md`, `CONTRIBUTING.md` and `doc/` before untracking, so nothing is lost by its absence. Purged from all 125 commits, which rewrote 91 of them; the 47 hashes cited in `doc/` were remapped and re-checked row by row against a pre-rewrite mirror, 0 mismatched, and a fresh clone from the forge holds no object under either path |
| 0.2.8 | 2026-08-26 | two changelogs held the same fact and had diverged inside a day: `CHANGES` stopped at v0.1.10 while the roadmap's table ran to 0.2.6 and was the one a gate read (`433fb5b`) | the four-column table survives as `CHANGELOG.md` at the root, `CHANGES` is deleted rather than synced, and `test-history.sh` was re-falsified at the new location: a duplicated version and an unresolvable hash each exit 1, a missing file exits 2 |
| 0.2.9 | 2026-08-26 | the documentation read as machine-generated: nine milestones filled out from one seven-heading template, a `Last reviewed` date under each, a revisions list duplicating `git log`, and a self-justifying clause under nearly every sentence in six files (`e1df2ed`, `67cdde3`, `83754a1`, `2b55dd4`, `51adbe7`) | the roadmap is 632 lines to 390, with 0.5 through 1.0 a paragraph each and the full treatment kept for the three milestones that can start. The gates' comment blocks keep their reasoning, which is what stops one being simplified into a check that passes on an empty population, and lose the capitals and the closing aphorisms; verified comments-only by diffing all nine with comment lines stripped. Calibrated against Kusumi's own `freebsd_hammer2` README and CHANGES rather than against an idea of what does not read generated |
| 0.2.10 | 2026-08-26 | the tree was written from the author's desk: twenty-three places said "on this workstation" or "on this machine", the roadmap published one libvirt host's inventory with domain names and a total of sixty, and `IO_MODEL.md` opened by narrating a store path, a binary cache and a `NarSize` to establish which kernel the gates read - untrue since the syntax gate began refusing any tree that is not the kernel of record (`70ea1ec`) | each deixis replaced by the fact behind it, the guests section rewritten as the four guests the milestones require with the one thing the inventory carried kept as a sentence, and the maintainer named by role |
| 0.2.11 | 2026-08-26 | `test-vectors-contract.sh` froze the crc32c wording and not the xxh64 wording, so the two strings Saxum's checkalg gate counts its vectors by, `XXH64` and `want`, were unfrozen. A rename here would have sent that gate red for an edit made in this tree (`df1e62d`) | one pattern anchored on the printf that emits both, plus the wording read out of the program's own output, since a printf reached under a condition that never holds passes every source check and prints nothing. Falsified separately for that reason: a rename fails the source check and the gate never reaches the output one. Ten checks, and `README.testing.md` enumerated three of the four promises |
| 0.2.12 | 2026-08-26 | the roadmap organized every milestone by stage names H0 to H7 and defined none of them; three source comments use them too, and the plan of record that defines them is in another repository this tree does not carry (`fd44016`) | the eight stages defined in place rather than cited, since a definition one directory away is unreachable at handover, which is what the tree is being restructured for. H0's surviving evidence named, and `H2` disambiguated: a stage beside a version number, the filesystem's own abbreviation in every carried comment |
| 0.2.13 | 2026-08-26 | every gate here reads a file that is not changing while it reads, so the testing document's whole account of controls is about whether an instrument can fail at all; the gates from 0.3 on watch a guest boot and a filesystem cut off mid-write, where a working instrument pointed before the phase under test reads exactly as clean as a healthy system (`a6103dd`) | a positive control required of any gate that samples a running system: something that must move during the phase, sampled in the same pass as the measurement. Relayed from a sibling repository, where a fix that worked was credited to a cause invented from `du` samples that all predated the write phase |
| 0.2.14 | 2026-08-26 | `hammer2_flush.c` was the last carried file and the only one whose port needs a decision rather than a shim: it flushes the device, and the three BSD ports do not agree on how (`a235a5b`) | its OS-dependent surface is one function, `hammer2_xop_inode_flush`. The cache flush follows NetBSD and OpenBSD, which both collapse it to a single ioctl, rather than FreeBSD's GEOM bio, because `blkdev_issue_flush()` is that same call; `VOP_FSYNC` on each device becomes `sync_blockdev()`, whose lack of a caller lock retired the `vn_lock` pair; the volume header write goes through this port's DIO layer rather than the buffer cache, reading the block before overwriting all 64 KiB because the read-skipping `getblk` is not exported there. Type-checks under clang and gcc with warnings as failures. Nothing is exercised |
| 0.2.15 | 2026-08-26 | four places still described `hammer2_flush.c` as unimported, including the roadmap's first next move and 0.2's third criterion, and the `XXX` count said six marks with none in a carried file (`ea3c2d5`) | 0.2 recorded as met, and the count remeasured at eleven. Five are in `hammer2_flush.c`, the first port edit inside a carried file, said in those words rather than absorbed into a total |
| 0.2.16 | 2026-08-26 | the style baseline moved when `hammer2_flush.c` landed and CI was the only instrument that could say so: the workstation's checker reports v7.2 and hashes differently, so the gate exits 2 there rather than charging a moved set to the code (`54aed53`) | 15 hits over 1,315 lines, 745 to 760, fourteen in categories already open. The fifteenth is upstream's `#if 0` around a disabled assert, kept, since deleting it to satisfy a checker is an edit to a carried file. The new numbers were produced by the checker the old baseline records, fetched and verified by `sha256` against its own first line |
| 0.2.17 | 2026-08-26 | `test-checkpatch.sh --write` recorded the checker's version and not its `sha256`, so a baseline written that way disarmed the gate's strongest check silently: an empty hash means the not-attributable branch cannot fire and the next moved set is charged to this tree whatever produced it (`7f67b13`) | both header lines written. Driven rather than reasoned about: with the baseline removed, `--write` reproduces the committed file byte for byte and the gate goes green against what it wrote |
| 0.2.18 | 2026-08-26 | `hammer2_flush.c` gave the shim two inlines taking a `struct file` the stub tree never declared, and the shim gate's verdict then depended on the compiler: GCC 13 on the runner accepted it, GCC 16 here warned, and warnings are failures, so `346dac6` was green in CI and red on the workstation (`f6c6c11`) | `file_bdev()`, `sync_blockdev()` and `blkdev_issue_flush()` transcribed from v7.2 `include/linux/blkdev.h` rather than written to suit the shim, and the driver calls the pair. The compiler stays unpinned: a gate whose point is needing nothing but `cc` is worth more with two opinions than with one, as long as a disagreement is read as a finding |
| 0.2.19 | 2026-08-26 | `test/syntax-check.c` and `script/test-shim.sh` both said every static inline in the shim is referenced by the driver, because an unused one is parsed and little else. Counting them said nine of forty-three were not, and the gate reads the same green either way (`dd5750a`) | the nine called, and the sentence made a check: the zone allocator's four, `hammer2_gfp`, `adjust_malloc_leak`, `hammer2_mtx_init_recurse`, `pause` and `tsleep`. It asserts the population first, since an awk matching nothing would report every inline covered, and was driven by deleting one reference |
| 0.2.20 | 2026-08-26 | three numbers in `doc/` had gone stale under the gates that produce them: the shim and syntax gates at 3 and 7 checks where they run 6 and 25, and the style deviation set quoted at 583 in two places where the baseline has read 760 since the carried core landed (`00ffb47`) | re-read off the gates rather than adjusted, and the testing file's prediction that the shim gate's unpinned compiler would bite through clang replaced by the two GCC versions that actually disagreed |
| 0.2.21 | 2026-08-26 | `hammer2_cluster.c` was on the not-here list beside seven files that all touch the OS, and it touches none of it: no vnode, no buf, no thread, no mount, one include (`390d817`) | carried byte-for-byte from the FreeBSD port at the commit its `provenance.csv` row names, re-verified by `cmp`, and compiled unedited under clang and gcc with warnings as failures on the first attempt. The carried set is seven files at 10,749 lines. The style baseline moved 760 to 764 under the checker of record: two SPDX hits, which every carried file adds, and two `return (x);` |
| 0.2.22 | 2026-08-26 | `test-provenance.sh` runs its `cmp` only where the carry column reads exactly `identical`, and anything else falls through to the count of rows it cannot falsify, green either way. `hammer2_cluster.c`'s row said `carry` for an hour and read as verified in the summary (`9e12ac5`) | the column's vocabulary checked before it is read, since a typo in it is invisible everywhere else, and driven by putting the typo back |
| 0.2.23 | 2026-08-26 | `hammer2_subr.c` is 450 lines of helpers with three spots that touch the host and one function Linux cannot have: all three BSD ports build the fsid by hashing the mount path, and a Linux filesystem is never told its mount path (`0b268c7`) | the timestamp becomes `ktime_get_real_ts64()`, following OpenBSD's port in dropping a precision sysctl Linux has no equivalent of; the signal check returns `EINTR` where the ports return a signal number, safe because every caller tests for non-zero and none reads the value; the two timespec functions take `timespec64`, which the carried header had already chosen. `hammer2_getnewfsid()` is left out under a `DEFER` naming its two candidates and the file where the choice belongs. `enum vtype` goes in the shim, values this port's own, which keeps both conversion functions readable as DragonFly's |
| 0.2.24 | 2026-08-26 | the style baseline moved 764 to 782, and three of the eighteen were a new category raised by this port's own `XXX` comment blocks rather than by a carried idiom (`0b268c7`) | those three fixed rather than baselined: the BSD-style exception covers what upstream wrote, not what this port writes beside it. The two other new categories are upstream's own lines and stay |
| 0.2.25 | 2026-08-26 | `hammer2_ondisk.c` is the first file whose OS half has no BSD answer to copy: FreeBSD reaches the device through GEOM, and `bdev_file_open_by_path()` collapses `namei()`, the disk check, the permission check and the open into one call (`412bc98`) | the verification half carried and the device half rewritten, with all seventeen `XXX` marks on the device side so the carried half reads clean against FreeBSD's. `hammer2_lookup_device()` and the three GEOM access helpers are not carried, and the init/open split is reversed from FreeBSD's because the one Linux call both resolves and opens. `hammer2_access_devvp()` answers what is left after the VFS and the open have answered the rest. The filesystem type uuid is compared as bytes against a constant written beside the string it equals, since the kernel has no `snprintf_uuid(9)` |
| 0.2.26 | 2026-08-26 | three comments and `doc/IO_MODEL.md` said the mount path would set the mapping's minimum folio order with `sb_set_blocksize()`, which is the wrong call: it acts on `sb->s_bdev_file`, and a HAMMER2 filesystem spans up to `HAMMER2_MAX_VOLUMES` devices of which only the root is ever `sb->s_bdev` (`412bc98`) | `hammer2_open_devvp()` calls `set_blocksize()` per device as it opens it and fails the open on a non-zero return, so the assertion `hammer2_io_folio_check()` has been making since it was written now has something behind it. The mount path still owes `sb->s_blocksize` |
| 0.2.27 | 2026-08-26 | the style baseline moved 782 to 856, and `doc/README.kernel-style.md`'s category table had six rows quoting counts from before the carried core landed, including one whose per-file breakdown named three files and summed to nine against a baseline of eleven (`412bc98`) | the table re-read off the baseline, and the `__inline` row recounted per file at twelve across five. Nothing in the move was fixed rather than baselined: the one new category is the errno convention reaching `EACCES` for the first time, and the eight new start-of-line hits were checked line by line and are BSD continuation indent |
| 0.2.28 | 2026-08-26 | `hammer2_cleanup_devvp()` asserted that the device was already closed instead of closing it, and `KKASSERT` is compiled out of the default build, so an unwind from a mid-loop failure in `hammer2_open_devvp()` would have held a block device open for the life of the module (`412bc98` fixed here) | the put made unconditional, which is what FreeBSD's `vrele()` in the same function is, with a `WARN_ONCE` on the path that should not be taken. This is the rule the porting document already states for OS-half code: `WARN_ONCE` plus recovery, not a new assert |
| 0.2.29 | 2026-08-26 | `412bc98`'s message, `doc/README.status.md` and the `provenance.csv` row all said every one of `hammer2_ondisk.c`'s seventeen `XXX` marks was on the device side. Counted by function it is nine to eight, and the claim was advertising a reviewability property the file does not have | the real split written down, with the eight in the carried half named one by one. All eight are one-line substitutions and no carried function had its control flow edited, which is a weaker claim than the false one and a checkable one |
| 0.2.30 | 2026-08-26 | the four `DEFER` markers each named their trigger beside their own code and nowhere else, so nothing collected them and nobody could see that three of the four lift with the same next move (`3e389e3`) | a ledger table in `doc/README.status.md`, read against `src/` by `test-inventory.sh` in both directions. The reverse direction is the one that needed a check: a marker deleted from the source leaves a row reading as outstanding work forever, and no other gate walks the ledger. Matched on the marker text verbatim, and both directions driven by making each break in turn |
| 0.2.31 | 2026-08-26 | three prose claims went stale as the imports landed, one of them in `CLAUDE.md`, which is read at the start of every session and said five of six carried files were in (`3e389e3`) | that one now names the instrument rather than a count, since a state claim in an always-loaded file misinforms every session started after it stops being true. The roadmap's remaining-work list still had `hammer2_flush.c` outstanding, and `README.md` said the core is carried unchanged, which stopped being true at the first `XXX` mark in a carried file |
| 0.2.32 | 2026-08-26 | `hammer2_cleanup_devvp()` warned on the path it exists to handle: `WARN` in Linux means this cannot happen, taints the kernel and kills a `panic_on_warn` box, and reaching that branch is an ordinary mount failure unwinding a partial `hammer2_open_devvp()` (`3717c64`) | the put stays unconditional, the report drops to `debug_hprintf`, and `hammer2.h` states the close-before-cleanup contract at the declaration so a later caller has something to be held to |
| 0.2.33 | 2026-08-26 | `hammer2_access_devvp()` infers the open mode from `f_mode`, and whether `bdev_file_open_by_path()` reflects `BLK_OPEN_WRITE` there was an assumption written nowhere: the kernel tree of record is headers only, so `block/bdev.c` cannot be read, and compiling says only that the field exists (`3717c64`) | a `DEFER` with the alternative named, which puts it in the ledger and under the check that reads it. `hammer2_init_devvp()`'s constant return said in place too, since the consequence is that a bad device path is diagnosed at open rather than at init |
| 0.2.34 | 2026-08-26 | the gate sweep this session ran was `bash "$g"; echo "$(basename $g)=$?"`, where `$?` belongs to `basename` and reads 0 whatever the gate did, so every "all nine green" reading it produced was about `basename` (`3717c64` is the work it was checking) | re-swept with the status captured first. The pushed head was in fact green; two line-count findings in uncommitted work were not. This is the same defect this session hit earlier in the same shape, both times while verifying rather than while authoring, which is where the rule against it is not indexed |
| 0.2.35 | 2026-08-26 | `hammer2_inode.c` was last on the import list because the VFS entry shapes its inode lifecycle, and that reasoning skipped a dependency: `hammer2_pfsalloc()`, which the VFS entry needs, calls four `hammer2_inode_*` functions (`61ae3a1`) | carried apart from `hammer2_igetv()` and the create path, both `DEFER`red with triggers rather than written blind. `hammer2_igetv()` is vnode lifecycle end to end and Linux's `iget5_locked()` is its four calls in one; the create path is `struct vattr`, `struct ucred`, `VNOVAL`, `groupmember()` and `priv_check_cred()`, and its `va_type` conversion is the same `S_IFMT` translation the `enum vtype` `DEFER` already waits on. The import phase is over: the four files left are rewrites |
| 0.2.36 | 2026-08-26 | three files said the one path that recursed is closed because NetBSD never sets `HAMMER2_OPFLAG_DIRECTDATA`. It closes the creation half only: the flag lives in the on-disk inode, so a filesystem written by DragonFly or a BSD port carries DIRECTDATA inodes whoever mounts them (`61ae3a1`) | `hammer2_chain_lookup()` reads the flag off the media and takes `down_read()` on an `rw_semaphore` it already holds, which is legal until a writer queues between the two. Reading a small file on a foreign filesystem is an open question for the read-only mount at 0.4, recorded as a reading of `hammer2_chain.c:2189` and the `LOCKAGAIN` branch it reaches, not as a run. This port sets the flag nowhere at all, since its only setter went with the create path |
| 0.2.37 | 2026-08-26 | the SYNCQ sleep in `hammer2_inode_lock()` sleeps on `&ip->flags`, an address FreeBSD's `msleep(9)` takes as a wait channel, and this port's `hammer2_lkc_t` is a `wait_queue_head_t`, which is an object rather than an address (`61ae3a1`) | the wait queue goes in the inode, beside the three `hammer2_pfs` already has. The alternative was the shim's `tsleep()`, whose own `XXX` says it ignores the wakeup: this site has a real waker in `hammer2_inode_unlock()`, so that mapping would have turned an exclusive inode lock into a poll with a tick of latency and said nothing about it |
| 0.2.38 | 2026-08-26 | the style baseline moved 856 to 882, and `hammer2.h` had never had a row in the `XXX` table although this port edits it in place (`61ae3a1`) | 26 hits over 1,482 lines, the sparsest since `hammer2_flush.c`, which is what a carried file looks like when the OS-touching functions are left out rather than rewritten. No new category. One hit fixed rather than baselined, a trailing `*/` on a comment this port wrote, the same one as at 782 |
| 0.2.39 | 2026-08-26 | the `enum vtype` `DEFER` named `hammer2_vnops.c` as its trigger, which is where the BSDs convert but not what fires the conversion: they reach `hammer2_vinit()` from `hammer2_igetv()`, and on Linux the type lands in `inode->i_mode` at `iget5_locked()` time, so it fires in whichever file holds the `igetv` replacement (`61ae3a1` is what prompted the recheck) | the trigger names the function, which is true whichever file it ends up in, and the row says it is one decision with the `hammer2_igetv()` row. The gate matches marker text and cannot check a trigger's truth, so that is a hand check at each import, said in the ledger itself |
| 0.2.40 | 2026-08-26 | two sentences written with the last commit claimed more than was measured: that the import phase is over because everything readable off a BSD port is in, and a per-category count of 26 style hits  that enumerated 25 (`211a27d`) | the first says what was measured, that the four remaining files are the OS-facing ones, and notes that how much of `hammer2_strategy.c`'s chain logic carries is a question for that file. The second names the twenty-sixth, one `__inline`. A count beside a list is the shape the project's own rule flags |
| 0.2.41 | 2026-08-26 | the `DEFER` ledger check wrote `/tmp/h2-defer.$$` two lines under a comment saying mktemp with a trap rather than `$$` in `/tmp`, because a predictable name in a shared directory is another user's to create (`df72ecf`) | routed through the mktemp directory the second loop already used. A comment asserting a property the code beside it lacks is worse than no comment, being the thing a reader checks instead of the code |
| 0.2.42 | 2026-08-26 | the `XXX` table is 0.2's fourth exit criterion and nothing checked it, although its total column is `grep -c XXX` and was checkable all along: it omitted `hammer2_disk.h` and `src/sys/sys/tree.h`, both of which carry a mark, while listing `hammer2_cluster.c` at zero, which is what turns a partial table into one that reads as an inventory (`e0aba33`) | the total and both directions of the population checked by `test-inventory.sh`, scoped as every `.c` and `.h` under `src/sys/fs/hammer2/` plus any file elsewhere under `src/` holding a mark, with three failure paths driven. Neither omission moves the fifty-two: both marks are their authors', Dillon's note on the reserved area and FreeBSD's own `XXXLAS`. The upstream and this-port columns are a subtraction against a tree absent from most machines, so gating them would exit 2 forever and they stay prose with a measurement date. Two prose counts went with it, one stale at six where the inode shim edits made it seven |
| 0.2.43 | 2026-08-26 | `CLAUDE.md`'s documentation duties enumerated four gated doc facts where the previous commit made it five, and a list that under-reports its own coverage is read as complete (`e0aba33` is what it fell behind) | the `XXX` table's row added, naming what is checked and what is prose. `doc/README.status.md` also says why `src/sys/sys/queue.h` has no row where `tree.h` does, since an absence produced by a rule and an absence that is a gap look identical to whoever greps for it |
| 0.2.44 | 2026-08-26 | `hammer2_vfsops.c` is the first file where the OS half has no BSD answer to copy at the level of the file rather than the function: FreeBSD's `hammer2_mount(struct mount *)` maps onto `->init_fs_context`, `->parse_param`, `->get_tree` and a fill-super, with `MNT_UPDATE` splitting off to `->reconfigure` (`2562bdd`) | the PFS half carried and separated so it stays readable against the three BSD ports, and the opening comment claiming less than `hammer2_ondisk.c`'s: statements carry, the function boundary moves, so the property that no carried function had its control flow edited is not asserted here. `hashinit(9)` becomes an array and two lines, since `HAMMER2_IHASH_SIZE` is a compile-time constant and there is nothing to size at run time. `MNT_WAIT` and `MNT_NOWAIT` are chosen to be Linux's `->sync_fs` wait argument rather than translated at the boundary |
| 0.2.45 | 2026-08-26 | whether HAMMER2 can mount through `get_tree_bdev()` was an open design question, and it is the same question `sb_set_blocksize()` got wrong: HAMMER2 spans up to `HAMMER2_MAX_VOLUMES` devices and `hammer2_open_devvp()` already opens all of them with the `super_block` as holder (`2562bdd`) | settled against the kernel of record rather than a prior. `get_tree_bdev()` runs `setup_bdev_super()`, which opens one device itself and ends in `sb_set_blocksize(sb, block_size(bdev))`, the device's block size and not `HAMMER2_PBUFSIZE`. btrfs, the multi-device filesystem in tree, uses `sget_fc()` with `set_anon_super_fc()`, `super_setup_bdi()`, no `sb->s_bdev`, and passes the `super_block` as holder to its own device opens, which is what this port already does. Neither kernel tree on this machine ships `fs/*.c`, so `fs/super.c` and `fs/btrfs/super.c` were fetched at v7.2 |
| 0.2.46 | 2026-08-26 | the syntax gate flagged `hammer2_mntlist` as defined and unused, because upstream keeps it in this file and its only user is the mount path (`2562bdd`) | dropped until the mount half lands rather than silenced, since the warning is the accurate signal that the file is a third written. `hammer2_pfsfree_scan()` keeps its `hammer2_vfs_sync_pmp()` call for the same reason: nothing links yet, and a symbol missing at link time is visible where a stub returning success would be silent on the path that decides whether an unmount lost data |
| 0.2.47 | 2026-08-26 | the style baseline moved 882 to 894 (`2562bdd`) | eleven of the twelve are the SPDX pair and the BSD idioms any new carried file adds, and the twelfth is the errno convention reaching a new call site, the same call as at 782: `hammer2_ipdep_init()` returns `ENOMEM` and not `-ENOMEM`. Nothing fixed rather than baselined, no new category. The gate returned COULD-NOT-RUN rather than a failure while the baseline was still the old one, which is correct: the auto-discovered checker's sha256 does not match the baseline's, so a differing deviation set is not attributable to the code |
| 0.2.48 | 2026-08-26 | the roadmap's next move 2 named seven entry points spanning three files, so any one file landing satisfies a third of the row while reading as the whole of it, and "where we are" said the mount path had not been started an hour after it had (`921e64f`) | the row says which file owns which entry point and what has started; the state sentence names `README.status.md` rather than carrying a claim that goes stale, which is the fix `CLAUDE.md` got at 0.2.31 |
| 0.2.49 | 2026-08-26 | `M_WAITOK` mapped to a plain `GFP_KERNEL`, which returns NULL, and eighteen carried call sites do not check because `malloc(9)` says it cannot. The only guard was a `KASSERTMSG` the default build compiles out, the same shape as the `KKASSERT` that stood in for a device close at 0.2.28 (`83a3ddf`) | `__GFP_NOFAIL` added on the waitok path, which is the kernel's own name for the contract and which `kvmalloc` honours: read at v7.2, `mm/slub.c` takes the flag directly below `PAGE_SIZE` and `kmalloc_gfp_adjust()` strips it above with "nofail semantic is implemented by the vmalloc fallback". Fixed in the shim rather than at eighteen sites, which is what the shim is for; a size above `INT_MAX` stays the residual and the asserts stay for it. `M_NOWAIT` still may fail, which is also `malloc(9)`'s contract |
| 0.2.50 | 2026-08-26 | `hammer2_ipdep_init()` checked `hmalloc()` for NULL under `M_WAITOK` and returned `ENOMEM`, an unreachable branch that bought a new checkpatch category, and `doc/README.kernel-style.md` had already recorded that category as the errno convention reaching a new call site (`83a3ddf`) | the branch removed and the baseline back from 894 to 891. The style doc says what happened rather than the reading it drew: a new checkpatch category is a question about the code before it is a property of it. Found by asking whether a failure the code handles can occur, which is what turned up the shim defect above |
| 0.2.51 | 2026-08-26 | `hammer2_access_devvp()`'s `DEFER` said `block/bdev.c` could not be read because the kernel tree of record here is a headers package. That is true of the tree and false of the question: every file is published at the tag `KERNEL_REF` pins (`ad8a9f5`) | traced at v7.2 and the assumption holds: `sb_open_mode()` gives `BLK_OPEN_WRITE` unless `SB_RDONLY`, `blk_to_file_flags()` turns it into `O_RDWR` or `O_WRONLY`, and `alloc_file()` sets `f->f_mode = OPEN_FMODE(flags)`. The `DEFER` is gone. `doc/README.porting.md` gains the general form, since compiling proves a field exists and never what sets it |
| 0.2.52 | 2026-08-26 | the `->statfs` `DEFER` offered `huge_encode_dev(sb->s_bdev->bd_dev)` as the alternative to the volume header's fsid, and that alternative stopped existing when the mount design took an anonymous super (`ad8a9f5`) | the trigger names the remaining choice and what to confirm before relying on it. `hammer2_ondisk.c`'s device-side marks are now enumerated one by one, since the old list named seven things for a count of ten |
| 0.2.53 | 2026-08-26 | `doc/IO_MODEL.md` argued against `sb_set_blocksize()` from the premise that only the root volume is ever `sb->s_bdev`, and `doc/ARCHITECTURE.md` said `hammer2_vfsops.c` does not exist; both were true before that file landed and neither is read by a gate (`d48c6d3`) | the premise corrected to what the mount design settled, that no volume is ever `sb->s_bdev` under an anonymous super, which strengthens the conclusion and gives the paragraph the other half of the trap: `get_tree_bdev()` runs `setup_bdev_super()`, which ends in `sb_set_blocksize(sb, block_size(bdev))`. The architecture note keeps the claim that still holds, no `file_system_type` and so no entry point, and points at `README.status.md` for the amount |
| 0.2.54 | 2026-08-26 | `hammer2_igetv()` was the last deferred piece standing between the tree and a mountable root, and it is the inode lifecycle decision the roadmap's next move names (`71051ab`) | written on `iget5_locked()`, which is FreeBSD's `vfs_hash_get()`, `getnewvnode()`, `insmntque()` and `vfs_hash_insert()` in one call, so the temp-release dance around two racing hash operations is gone rather than translated. It stays in `hammer2_inode.c`, the file upstream keeps it in. Identity is the `hammer2_inode` pointer and not the inum, which is exact because `hammer2_inode_get()` interns one per `(pmp, inum)`; comparing inums would re-implement that hash beside it. The contract differs from the BSD ports' and `hammer2.h` says so at the declaration: an inode with a reference, no HAMMER2 lock, already out of `I_NEW`, where theirs returns a locked vnode |
| 0.2.55 | 2026-08-26 | three properties of the 7.2 inode API were assumptions until they were read (`71051ab`) | `iget5_locked()` takes a `u64` hashval, so a 64-bit inum is not truncated on the way in. `i_state` became a struct, so `I_NEW` is read through `inode_state_read_once()`, which is the idiom `fs/ext4/inode.c` and `fs/isofs/inode.c` use at that tag; `inode_state_read()` asserts an `i_lock` this caller does not hold. And every failure exit after `I_NEW` is `iget_failed()` rather than `iput()`, since `fs/bad_inode.c` defines it as `make_bad_inode()` plus `unlock_new_inode()` plus `iput()`, and an `I_NEW` inode released without that leaves every waiter in `__wait_on_freeing_inode()`: a mount that hangs rather than fails, and a hang writes no report. The `i_state` change is what a gate compiling against the latest release buys |
| 0.2.56 | 2026-08-26 | the `enum vtype` `DEFER` lifted exactly where its trigger said it would, and one new deferral replaced the two that went (`71051ab`) | the conversion is `hammer2_vtype_to_ifmt()` beside `hammer2_igetv()`, at the one place an inode is constructed, with `hammer2_get_vtype()` unchanged as the carried half; the reverse direction belongs to the create path and has no caller. The new row is `->evict_inode`, which owes the other half of the set callback: it takes the inode's reference on the `hammer2_inode` and nothing returns it yet, so every inode constructed today leaks one, including down `iget_failed()` |
| 0.2.57 | 2026-08-26 | the style baseline moved 891 to 898, and the `ENOMEM` category returned hours after being removed as unreachable (`71051ab`) | four `return (x);`, one start-of-line space, one `EINVAL`, one `ENOMEM`. The `ENOMEM` is reachable this time: `iget5_locked()` returns NULL when it cannot allocate. Same category, opposite verdict, and `doc/README.kernel-style.md` records both readings next to each other, because what separates them is whether the failure can occur and not whether the code handles it |
| 0.2.58 | 2026-08-26 | next move 2 said `hammer2_igetv()`'s replacement is what `super_operations` waits on, which stopped being true one commit earlier (`6755996`) | the row says the decision is made and names what it owes back, `->evict_inode`, which is in the ledger rather than in prose |
| 0.2.59 | 2026-08-26 | the module entry: `module_init`, `file_system_type`, `register_filesystem`, and the globals every carried file had been reading through an `extern` that nothing defined, so nothing in this tree could ever have linked (`c4003f0`) | `script/test-syntax.sh` under both compilers, which needed one Kbuild define, `CC_USING_FENTRY`, before any file could include `<linux/module.h>`; `hammer2_get_tree()` fails every mount rather than stubbing a success, and `hammer2_mntlist` stays undefined so the gate keeps reporting the file part written |
| 0.2.60 | 2026-08-26 | four port decisions recorded with the entry: the sysctl block becomes `module_param_named()`, `hammer2_assert_clean()` moves from load to unload where zero can mean a leak rather than an empty module, `uma_zcreate(9)`'s cannot-fail assertion becomes an error path because `kmem_cache_create()` can, and `desiredvnodes` becomes `totalram_pages()` with upstream's clamp unchanged (`c4003f0`) | `doc/README.porting.md`, one section per decision; the baseline moved 898 to 902, four `return (x);` and no new category |
| 0.2.61 | 2026-08-26 | the style doc's category table kept its own copy of counts the baseline already gates, and every row of it was stale: the SPDX pair 14 and 14 against 15 and 15, the errno row 48 across 13 against 50 across 14, `return (x);` 132 against 140 (`5d983ae`) | the count column is gone and the table carries dispositions only, so `doc/checkpatch-baseline.txt` is the single place a number lives and `script/test-checkpatch.sh` is the only thing that has to agree with it |
| 0.2.62 | 2026-08-26 | the four allocation counters and `supported_version` were exposed as 0444 module parameters, which is read-only in sysfs and still settable at `insmod`: it reopened the hole `hammer2_assert_clean()` had just been moved to the unload path to close (`83dc0e3`) | `include/linux/moduleparam.h` at the kernel of record, which states that `perm` is visibility in sysfs and that the name becomes a command-line parameter regardless; nine tunables stay, the counters wait for `/sys/fs/hammer2/`, and the check now runs before the two `uma_zdestroy()` calls rather than after |
| 0.2.63 | 2026-08-26 | `->parse_param` and the option table: FreeBSD's numeric `hflags` becomes one named flag, `emerg`, because `HMNT2_LOCAL` is the only other bit and `hammer2_mount()` rejects it outright (`0102ba4`) | `fs/fs_context.c` at the kernel of record for the `-ENOPARAM` fallthrough that hands `source` to the generic handler; the style baseline does not move, and the one new category, `Prefer kzalloc_obj over kzalloc with sizeof`, was fixed rather than baselined because it is about code this port wrote |
| 0.2.64 | 2026-08-26 | the teardown path carried and wired to `->kill_sb`: `hammer2_mntlist`, `hammer2_unmount_helper()` and `hammer2_unmount()`, with `vflush()`, the return value and `MNT_FORCE` dropped because `kill_anon_super()` runs first and has already done that work (`a5c60fb`) | `fs/btrfs/super.c` at the kernel of record for the order; the three `hammer2_vfs_sync_pmp()` calls stay undefined on purpose, and the baseline moved 902 to 906 on four hits that are all upstream's own text |
| 0.2.65 | 2026-08-26 | the syntax gate cannot catch an unreachable static function, because `-Wno-unused-function` is in its flags for the carried files' sake, so the rule that no helper lands ahead of its caller is recorded as a discipline where it had been read as a check (`a5c60fb`) | `script/test-syntax.sh`'s own flag list; an unused static *variable* still fails, which is what `hammer2_mntlist` was demonstrating until this commit gave it a user |
| 0.2.66 | 2026-08-26 | `hammer2_init_devvp()` recorded paths and resolved nothing, on the reading that `bdev_file_open_by_path()` does the whole of FreeBSD's `namei()`; it does the open, and not the two things the vnode is there for before one: a bad path diagnosed before the mount lock, and a `v_rdev` to match a second mount of the same device against `hammer2_mntlist` (`9352078`) | `lookup_bdev()` resolves without opening, and `struct hammer2_devvp` gains `devno`. The two cannot be one call, because `bdev_file_open_by_path()` claims the device for a holder and `bdev_super_lock()` in `fs/super.c` dereferences that holder as a `struct super_block *` and then requires `s_root` and `SB_ACTIVE`; btrfs orders it the same way. Both read at the kernel of record |
| 0.2.67 | 2026-08-26 | the device half of the mount path, which had been an `EINVAL` with a `DEFER` on it: `fc->source` split on `@`, an anonymous superblock, the `mntlist` match, `hammer2_open_devvp()`, `hammer2_init_volumes()`, the `hmp` with its vchain and fchain, the super-root chain and the `spmp` iroot read off it, then `hammer2_update_pmps()` and a full teardown (`08a6357`) | `sget_fc()` takes a NULL test function on purpose, since upstream already answers "is this PFS mounted" with `pmp->mp` and a second answer could disagree with the first. `hammer2_recovery()` and `hammer2_fixup_pfses()` are deliberately not carried yet: both write, and no mount can succeed, so the named gap is that a read-write mount does not replay an interrupted flush. Baseline 906 to 911, no new category, and the one repeated word is upstream's, either side of a line break as the last one was |
| 0.2.68 | 2026-08-26 | the forward declaration the device half needed found the other half of a gate that had just been written off: `-Wno-unused-function` hides a static defined and never called, but a static *declared* and never defined is `-Wundefined-internal` under clang and "used but never defined" under gcc, and the syntax gate caught the missing carry of `hammer2_update_pmps()` on the first run (`08a6357`) | 0.2.65 recorded the no-unreachable-helper rule as a discipline rather than a check, which is still true; the file's opening comment now says which half of the pair is on. A conclusion about an instrument reached from one direction was wrong in the other |
| 0.2.69 | 2026-08-26 | `hstrdup()` called `kstrdup(str, GFP_KERNEL)` and could return NULL, the one allocator left outside the `M_WAITOK` contract `hammer2_gfp()` exists to keep; three callers already dereferenced it unchecked, and the mount path made it four (`bd77d9f`) | fixed in the function rather than at the call sites, where the contract lives. Found by asking, of the device half, what a fresh-install failure of each new allocation would look like; `hammer2_pfsalloc()` would have handed a NULL `pfs_names[]` entry to `strcmp()` |
| 0.2.70 | 2026-08-26 | `hammer2_vfsops.c`'s `XXX` prose said "five" and had said it since the file held seven marks; it holds twenty. The file-by-file summary above it did not sum to the column it claimed to decompose, and never had (`08a6357`) | both rewritten as a partition that adds up, with the summary saying in as many words that the paragraphs below it are not one. Only the total column is gated, which is why a per-file prose count could drift through six commits |
| 0.2.71 | 2026-08-26 | `doc/README.porting.md` said `hammer2_mount_helper()` "lands with `->get_tree`", which stopped being true the moment the device half of `->get_tree` landed without it, and repeated the syntax gate's blind spot without the half that is not blind (`3472303`) | the trigger is now the PFS half specifically, and the paragraph records both directions: silent about a helper that arrives early, loud about one declared and never defined. A conclusion about an instrument reached from one direction was wrong in the other, and the doc had the same defect the source comment did |
| 0.2.72 | 2026-08-26 | the sentence written in 0.2.70 to stop a count going stale went stale in the same commit and in two ways: it said the paragraphs below walk "the three largest sets" while skipping the second largest, `hammer2_inode.c`'s seventeen, and it carried the literal seventy-two after `hstrdup` moved the total to seventy-three (`3472303`) | the file now names which three files are walked, says forty-one of seventy-three are in those paragraphs, names the thirty-two that are not and why they need not be, and says in as many words not to read them as a decomposition. A summary of a gated number is a second copy of it, whatever it is phrased as |
| 0.2.73 | 2026-08-26 | `doc/README.porting.md` said `hammer2_mntlist` "is still not defined", which stopped being true when the teardown path gave it a user two commits earlier (`a5c60fb`) | the paragraph keeps the reasoning, which is reusable, and moves the state into the past tense with its date. This is the third state-claim correction in two days and all three were sentences describing what did not exist yet, which is the shape that rots fastest |
| 0.2.74 | 2026-08-26 | `doc/IO_MODEL.md` said the folio-order helpers "are the right call sites when the mount path lands", written before `hammer2_open_devvp()` called `set_blocksize()` for real, which reaches one of them by itself (`08a6357`) | read at the kernel of record: `bdev_validate_blocksize()` checks the block-size range and the device's logical block size and nothing about folios, and `mapping_set_folio_order_range()` returns silently without THP and clamps silently with it, so `set_blocksize()` returns 0 on a kernel that cannot supply the folio. The refusal has to be explicit because the open path cannot report the condition; it stays roadmap item 3 rather than a `DEFER`, since the `static_assert` fails the build without THP and `hammer2_io_folio_check()` re-checks every read, and the clamp case is unreachable on any configuration this module builds on |
| 0.2.75 | 2026-08-26 | the comment proving the device half writes nothing said the `FLUSH_MASK` bits are "set in nine places", which reads as nine sites; there are eleven sites in nine functions (`4111726`) | recounted mechanically rather than from the earlier reading, and the comment now names both numbers and how they were obtained, since a count beside a list is the shape that goes wrong quietly. The conclusion is unchanged: none of the nine is reachable from the device half |
| 0.2.76 | 2026-08-26 | 0.2.72 through 0.2.74 were committed after running the inventory gate alone, and 0.2.74 pinned no commit hash, which `script/test-history.sh` would have said before the push (`4111726`) | the hash is pinned. Nine gates exist because no one of them answers for another, and running the one that was complaining is not the same as running the set |
| 0.2.77 | 2026-08-26 | the PFS half of `hammer2_get_tree()` and the fill-super: label lookup under `spmp->iroot`, `hammer2_pfsalloc()`, `hammer2_mount_helper()`, `hammer2_sops` with `->evict_inode`, `hammer2_igetv()` for the root inode, and `d_make_root()` (`b78c061`) | `script/test-syntax.sh` under clang and gcc with warnings as failures; `DEFER(super_operations gains ->evict_inode)`, `DEFER(the PFS half of hammer2_get_tree() lands)`, and `DEFER(the PFS half of this function lands)` are lifted. `doc/README.status.md` and `doc/README.porting.md` updated, style deviation set unchanged |
| 0.2.78 | 2026-08-29 | `doc/research/` and `doc/upstream/` moved here from Saxum, on the rule that a component with its own repository owns its own development, and `script/test-doc-prose.sh` moved with them because prose taken out of a governed tree and left ungoverned is the defect the gate was written for (`0fe90e2`) | `script/test-doc-prose.sh` and `script/test-inventory.sh`; the gate asserts a non-empty population before it reads anything, so a sweep matching nothing cannot pass |
| 0.2.79 | 2026-09-02 | `script/test-doc-prose.sh` returned vale's own status, which is nonzero for errors only, so twelve findings printed and the gate passed on every run it had ever made; CI installed no vale, so the same gate reported COULD-NOT-RUN on every push | the gate counts findings itself and fails on any; CI installs vale 3.18.0 pinned by sha256, for the reason checkpatch is pinned to the kernel of record. The twelve are fixed in the commit before it. The failing direction was driven by hand: a one-line document with a British spelling turns the run red and its removal turns it green. `doc/README.testing.md` also claimed `test-citations.sh` reads every `file:line` citation in `doc/` where it reads those inside a table row, and now prints how many it left alone (`ff5c6ba`) |
| 0.2.80 | 2026-09-02 | no gate had ever seen a built tree, and the first `make` put thirteen objects beside the sources: `script/test-provenance.sh` asked for an origin row for each of thirty files kbuild had written, and `script/test-inventory.sh` read `XXX` out of the strings in `hammer2.o`. Both failed loudly, so nothing was recorded wrong, but a verdict that turns on whether anyone ran `make` is not a verdict about the code | four gates now exclude kbuild's output. `script/test-checkpatch.sh` and `script/test-citations.sh` carried the same shape untripped; the one that bites later is `hammer2.mod.c`, which enters a `*.c` glob and which no build has reached because the link fails first (`cb33ca6`) |
| 0.2.81 | 2026-09-02 | the first build emitted seven warnings, five `-Wimplicit-fallthrough` in `hammer2_chain.c` and two `-Wunused-function` in `hammer2_inode.c`, and `script/test-syntax.sh` carried neither flag while advertising warnings as failures, so the gate was weaker than the build it stands in for | both flags added, suppressed on the carried files and fatal on ours, which is the split the gate already makes for `-Wno-pointer-sign`. Driven by giving `hammer2_io.c` one unannotated fallthrough and two unused statics, which turns three checks red. `src/sys/fs/hammer2/Makefile` suppresses the same two on the same files so the gate and the build agree about which text is DragonFly's, under `DEFER(the tree is prepared for submission)` (`44f48f6`) |
| 0.2.82 | 2026-09-02 | the status file opened by saying no module had been built, which the first `make` made false | the result is recorded instead: twelve objects, no warnings, four undefined symbols out of modpost, three of them `hammer2_strategy.c`'s and the fourth the deliberate one whose reasoning sits at its call site. The four citations sit in the column `script/test-citations.sh` reads, so the line numbers are checked. 0.3 was blocked on authorization to compile and is now blocked on the link (`d2b1007`) |
| 0.2.83 | 2026-09-02 | the comment beside the new `-Wno-unused-function` suppression said `hammer2_inode_lock_temp_release()` and `_restore()` were carried ahead of the write path that would call them. Their only caller in either upstream is `hammer2_igetv()`, the one function this port rewrote, so no caller is coming. `doc/README.status.md` also still said in its second half that no module had been built and that there is no VFS layer and no mount path | the reasoning already in the tree at that rewrite is cited instead: `iget5_locked()` does the lookup, the allocation and the insert in one call, so the dance those two perform has nothing to race against. They stay because deleting from a carried file is a core edit. The three stale sentences are corrected (`0dff50e`) |
| 0.2.84 | 2026-09-02 | the fix teaching four gates to skip kbuild's output had nothing standing behind it, and no gate had ever run against a built tree | CI builds the module before it runs any gate, so every gate meets the tree a developer has. The step asserts the build is warning-clean and undefined on exactly the four symbols the status table names, which turns a fifth reference into a red run. Both failing directions were driven locally by giving `hammer2_io.c` a call to an undefined function and then an unused static, under `DEFER(the module links)` (`8339e2a`) |
| 0.2.85 | 2026-09-02 | the `DEFER` ledger check walked `src/` alone, so `DEFER(a userland file is imported into the module tree)` in `script/hammer2-provenance.py` had never had a row, and the CI step's own trigger would have had nowhere to be collected | the population is `src/`, `script/` and `.github/` in both directions, driven each way. The pattern gained a three-character floor because the gate's summary line ends with `DEFER(s)` and the widened search matched the command carrying it (`384d748`) |
| 0.2.86 | 2026-09-02 | `hammer2_xop_strategy` carried a `struct bio *`, taken at the initial import from the H0 API map's row pairing BSD's `struct bio` with Linux's. The field is the destination of a logical file read; a `bio` is the block layer's request object, the layer beneath `hammer2_io.c` rather than the one above it, and no caller that can produce this xop holds one | it is a `struct folio *`. Nothing had used the field. Recorded in `doc/README.porting.md` because the rest of that map was written before the DIO layer existed and pairs types by name (`5c59f1d`) |
| 0.2.87 | 2026-09-02 | modpost reported four undefined symbols and the module could not be loaded: the two strategy XOP handlers named by `hammer2_admin.c`'s carried descriptor table, `hammer2_dedup_clear()` called from `hammer2_bulkfree.c`, and `hammer2_vfs_sync_pmp()`, which had been declared and left undefined so the absence would show at link time | `hammer2_strategy.c` lands with `hammer2_dedup_clear()` carried and both handlers as floors that warn once and feed an error through the XOP protocol; `hammer2_vfs_sync_pmp()` becomes a floor on the narrower argument that both its call sites discard the return value, so a warning in dmesg is the only loud channel either way. The kernel's own `LZ4_decompress_safe()` and `zlib_inflate()` family are exported at v7.2, so the read path needs no vendored compression. `make` produces `hammer2.ko`, warning-clean, `Dual BSD/GPL`, alias `fs-hammer2`, no module dependencies, which is 0.3's first criterion; the style baseline moves 911 to 913 on the floor's positive `EOPNOTSUPP` (`f592385`) |
| 0.2.88 | 2026-09-03 | `hammer2_get_tree()` began returning success, and four places went on saying it could not. `DEFER(a mount can succeed)` argued its own gap was harmless because every mount failed a few lines below; the ledger row and `hammer2_strategy.c`'s header repeated the premise; `README.status.md` said the module does not link one section after saying it builds warning-clean, and counted four floors where three exist | the deferral is `DEFER(the module is loaded and mounts a device)`, named for what lifts it, and says the gap is live with only the absence of a loaded module in front of it. The fourth floor was `hammer2_dedup_clear()`, which is carried. One of the three is reachable: `->kill_sb` reaches `hammer2_unmount()`, which calls `hammer2_vfs_sync_pmp()` three times. The issue template told a reporter that nothing mounts, and the roadmap said no module has been built (`9d0f54f`) |
| 0.2.89 | 2026-09-03 | the mount path began returning success while `hammer2_recovery()` and `hammer2_fixup_pfses()` remained uncarried, so a default mount, which is read-write, would have continued writing to a filesystem whose last flush was cut short. A `NULL` `->reconfigure` does not help: `reconfigure_super()` calls the operation only when present and applies `SB_RDONLY` under `sb_flags_mask` either way, so `remount,rw` would have cleared the flag with nothing consulted | `hammer2_get_tree()` returns `EROFS` for a read-write mount before the device is opened, and `hammer2_reconfigure()` returns it for the remount. Both lift with the recovery they stand in for, so the three sites share `DEFER(flush recovery lands)` and the ledger gives them one row. The file's `XXX` walkthrough said fourteen while enumerating fifteen sites (`75eeacb`) |
| 0.2.90 | 2026-09-03 | the mount path did not replay an interrupted flush, because `hammer2_recovery()`, `hammer2_recovery_scan()` and `hammer2_fixup_pfses()` were never carried, so the deferral at that site had nothing behind it | all three carried verbatim from the FreeBSD port with their two structures, the forward declarations and `HAMMER2_RECOVERY_MAXDEPTH`, and called where upstream calls them. Every symbol they need was already in the tree and the `TAILQ` macros are in the vendored `queue.h`. The recursion is bounded at ten frames whatever the topology is, deeper work being deferred onto a list drained at depth zero. They write, and nothing has been loaded, so the read-write refusals stay and the deferral names exercising them on a device. The style baseline moves 913 to 919 (`8b5c529`) |
| 0.2.91 | 2026-09-03 | `hammer2_fixup_pfses()` skips a non-inode chain with a bare `continue` inside `while (chain)`, which returns to the condition without advancing and without releasing the super-root inode lock. DragonFly, FreeBSD, NetBSD and OpenBSD carry it identically. `hammer2_chain_lookup()` without `MATCHIND` recurses into indirect blocks rather than returning them, so only a damaged image reaches the branch, which is what mount-time recovery meets | carried unchanged, since the rule keeping the four trees readable side by side has no exception for defects and the refusals mean nothing calls it. The fix is staged in `doc/upstream/` as two patches, verified to apply at zero fuzz against all four trees, for James to file (`7c7f941`) |
| 0.2.92 | 2026-09-03 | the `DEFER` ledger check matched a trigger anywhere in `README.status.md`, so a sentence of narrative prose naming an old trigger kept a marker green after its row had been rewritten. Two of the three read-write sites still carried `DEFER(flush recovery lands)` after the third moved to `DEFER(recovery is exercised on a device)`; the status document said the mount path runs without recovery, which the carry had made false; and both refusals told the user in dmesg that flush recovery is not implemented, when it is carried and has only never been run | the check reads table rows only and asserts it found some, so a document with no table cannot report clean. Driven both ways, including the exact shape that passed: a row renamed with the old trigger left in prose elsewhere. All three sites carry one trigger, and the messages say never exercised (`44ab959`) |
| 0.2.93 | 2026-09-03 | scoping the `DEFER` ledger check to table rows left the origin table inside its population, whose third column is free prose describing what each file defers, so a row there naming a trigger would have satisfied the check for a marker whose ledger row had been deleted. The roadmap still said what remains includes unblocking read-write recovery and writing a `->reconfigure` | the check anchors on the ledger's own column shape, a trigger in the second column, the way the citation gate anchors on a citation in the first. Driven on that exact shape and on a document with no ledger rows at all. The roadmap says the recovery is carried and called and that what remains is exercising it and lifting the two refusals (`50d7c2f`) |
| 0.2.94 | 2026-09-03 | thirteen sentences asserted a state the code had moved past over two days and eleven gates were green throughout, because no gate reads prose for truth. The checkable subset is the claims that name a symbol | `script/test-absence.sh` resolves every "X() is not carried" against `src/` and fails when the symbol is there. It flattens each file first, since these claims wrap across lines and a line-at-a-time pattern is blind to a continuation, which is how a hand sweep missed one of the thirteen. It asserts its population, carries a control that must extract and resolve a symbol on every run, and names the claims it does not read. Its first run found `hammer2_igetv()` called uncarried after this port rewrote it (`634569f`) |
| 0.2.95 | 2026-09-03 | `hammer2_igetv()` reads `i_state` through `inode_state_read_once()`, which arrived in 6.19, while the floor is 6.15, so on four releases the module failed as an implicit declaration in the middle of a compile rather than at the `#error` that exists to say a kernel is too old. CI had been red on it for seventeen consecutive runs, since the module build step landed, and sixteen pushes went out without the result being read | `hammer2_os.h` defines it below 6.19 as `READ_ONCE`, which is the same read: `i_state` is `u32` at v6.15 through v6.17, an enum at v6.18, and a struct behind accessors at v6.19, each dated by reading `include/linux/fs.h` at the tag. The module builds warning-clean at 6.18, where that definition is what compiles, at 7.1.9, and at the kernel of record under clang with `LLVM=1`, which had never been linked against. Baseline 919 to 920 on the second version guard (`ac27f89`) |
| 0.2.96 | 2026-09-03 | `kzalloc_obj()` is a 7.0 spelling used against a 6.15 floor, the second of the same kind as `inode_state_read_once()` and found the same way, by CI failing to build at 6.17 | guarded in `hammer2_os.h`. The guard is `#ifndef` and not a version comparison, because stable series backport it: Arch's 6.18.46 has it where mainline v6.18 does not, and a version guard redefined it there, which `-Werror` turns into a failure. Rather than chase these one CI round-trip at a time, all sixty identifiers called under `src/sys/fs/hammer2` that the tree does not define were resolved against a mainline v6.15 `include/` tree; two were absent and both are now guarded. The sweep sees an absent symbol, not a changed signature (`b9184ab`) |
| 0.2.97 | 2026-09-03 | the CI build step matched a bare `warning:`, which also matches kbuild's banner about the runner's compiler differing from the kernel's, so the step failed for two runs while the build under it was clean | the pattern requires `file:line:column`, which every compiler diagnostic carries and that banner does not, and a warning raised inside a kernel header still matches since this code is what includes it. The pattern is checked against a line built to match before it is trusted on a log that should have none. CI is green for the first time in eighteen runs, ten gates running and the provenance gate COULD-NOT-RUN (`6d8dcce`) |
| 0.2.98 | 2026-09-03 | the floor sweep that found `kzalloc_obj()` was a throwaway, and its own first run under-reported: the prototype pattern read an assignment whose right-hand side is a call as a declaration, so `kzalloc_obj()` was filtered out of its findings and was noticed by hand. The shim's `kzalloc_obj` takes one parameter where the kernel's is variadic | `script/floor-symbols.py`, with the pattern requiring a line carrying no assignment and a control that plants a name no kernel defines and requires it to surface. It is not a gate: it needs a floor tree, so it runs when the floor moves while CI at 6.17 is continuous. The arity is deliberate, `default_gfp()` being equally absent at the floor, and a call passing flags fails below 7.0 with a diagnostic naming the macro, measured rather than assumed (`7eeeaeb`) |
| 0.2.99 | 2026-09-03 | the version-floor section called 6.15 "no longer entirely unexercised" and supported it with builds at 6.18, 7.1.9 and the kernel of record, none of which is the floor. Nothing has ever compiled against 6.15; the lowest continuous build is CI at 6.17, two releases above it (`e583af6`) | the two-release gap named rather than described as coverage, and `script/floor-symbols.py` recorded as the only reader of the floor tree, resolving names without compiling, so a changed signature or a macro that stopped expanding is invisible to it |
| 0.2.100 | 2026-09-03 | `hammer2_os.h` asserted a 6.15 kernel floor with an `#error` and nothing had ever put a compiler on it, the lowest build being CI at 6.17. The floor was an assertion, and it had already failed twice as one, `inode_state_read_once()` and `kzalloc_obj()` both surfacing as implicit declarations mid-build against a newer kernel rather than at the `#error` written to report a kernel that is too old (`4432c1e`, `f58bb6c`, `2fdac27`, `62e7222`) | a second CI job builds the 6.15 tarball once, caches the tree on the immutable tag and links the module against it, asserting the tree reports 6.15 and carries a symbol table before it builds. Its first run found `SHA256_CTX` typedef'd to `struct sha256_ctx`, which the kernel called `struct sha256_state` until 6.17: same function names, same argument order, so `script/floor-symbols.py` resolved all three and reported nothing. The module links warning-clean at 6.15.0 against 12591 symbols |
| 0.2.101 | 2026-09-03 | the CI build step's warning check lived inline in one job and was about to be copied into a second, where it could rot in one copy while the other stayed right; the testing document said that step asserts the undefined symbol set is exactly the four in the status document, which nothing has ever done; and the read path's LZ4 and zlib dependencies were recorded as present at the kernel of record, which is the weaker of the two questions the SHA-256 rename had just answered (`4432c1e`, `f58bb6c`, `1a369d5`) | `script/build-check.sh`, one copy with the control the warning pattern needs, its three failing directions driven; the testing document says what the check does instead; and `<linux/lz4.h>` and `<linux/zlib.h>` compared byte for byte at v6.15 and v7.2, identical, so those four declarations cannot have moved inside the supported range |
| 0.2.102 | 2026-09-03 | `hammer2_igetv()` set `i_mode` and stopped, so `d_make_root()` was handed a directory with a NULL `i_op`, no size, no owner and no times: a mount that succeeds onto a mount point that cannot be entered, read, or stat'd correctly. The read path was planned ahead of this and could not have run, `->read_folio` being reached through a regular file's inode and that inode through `->lookup` on its parent (`dea2f6f`) | `hammer2_vnops.c` with `->lookup`, from upstream's `hammer2_lookup()` with the dcache's own "." and ".." cases dropped and the nameiop pre-checks dropped, the nresolve XOP unchanged in shape, and ENOENT returned as a negative dentry rather than an error. `hammer2_igetv()` fills every field a stat reports, the timestamps through `inode_set_ctime_to_ts()` and its siblings, all three declared at v6.15 and v7.2. Style baseline 920 to 922, both hits in recorded BSD-style categories |
| 0.2.103 | 2026-09-03 | the operations wiring set `i_op` and left `i_fop` NULL, which `do_dentry_open()` in `fs/open.c` takes through `WARN_ON` before failing the open with `ENODEV` at v6.15 as at v7.2, so the first `ls` on a mount point would have printed a kernel warning rather than failing quietly; `i_blocks` was unset, and `generic_fillattr()` copies it into `stat->blocks`, so `stat` and `du` reported every file as occupying nothing; and `meta.nlinks` was passed through unchanged, where `set_nlink(0)` tells the VFS an inode is unlinked and pending eviction and `hammer2_disk.h` documents that field as typically meaningful only for directories (`fee0c94`) | a `file_operations` for each type, the directory one without `->iterate_shared` so `iterate_dir()` reports `ENOTDIR` and the regular-file one empty so a read fails `EINVAL`, neither warning; `i_blocks` from upstream's `va_bytes` in 512-byte units; and a zero link count warning once and reporting one link |
| 0.2.104 | 2026-09-03 | twenty-six CI runs failed between 2026-08-29 and 2026-09-03, twenty-one at a module build, three at the repository gates and two at the floor job's own symbol-table assertion, all but two of them the tree's fault rather than the gate's. This row said twenty-five and said every one was a module build, both from a five-run sample rather than a count; the tally above was taken from the API. The volume was a working habit: CI was being used as a compiler, one push per question, each answer arriving as a failure notification. `doc/README.roadmap.md` separately said the Linux guest 0.3 needs does not exist, and supported a claim that nothing here drives a guest with a word count that was false when written (`013d8a6`, `3701c64`) | the floor reproduces locally, `script/build-check.sh` taking a `KDIR` and a built 6.15 tree answering in seconds what CI answers in four minutes, written into `doc/README.testing.md`; and the guest section corrected against `virsh list --all`, which returns sixty guests |
| 0.2.105 | 2026-09-03 | the guest table made `CONFIG_PROVE_LOCKING` a condition of loading the module, which is what had 0.3 recorded as blocked on a machine that had to be built. Loading and unloading are 0.3's two open criteria and a stock kernel runs them (`3701c64`) | one row for each claim, and the lockdep half measured rather than supposed: five kernel configs read out of guest images with `virt-cat` and no guest booted, Fedora 44 at 7.1.6, Nobara at 7.1.3, Void at 6.18.42, Gentoo at 6.18.41 and the host at 7.1.9, none with it set, four further guests unread rather than absent. Nobara and Void are inside the range this module compiles for, so the loading criterion is exercisable now |
| 0.2.106 | 2026-09-03 | `->lookup` rejected a name longer than `HAMMER2_INODE_MAXNAME`, where both call sites in `hammer2_inode.c` assert strictly less than it, that constant being the size of the on-media filename array; a name of exactly that length passed the guard and would have tripped the assertion below it (`4eb75d0`) | the comparison written the way the core writes it, with a note that the VFS rejects anything past `NAME_MAX` first so the guard is unreachable through a path walk and is kept as the core's bound rather than as a reachable check |
| 0.2.107 | 2026-09-03 | nothing this port has written had ever been inside a running kernel, and the module did not build at all against 7.3, where `fs_holder_ops` is static to `fs/super.c` and the holder is registered as a `{device, superblock}` pair rather than being the superblock (`9a96aad`) | the device open guarded on the version and every release routed with it, since `bdev_fput()` still compiles at 7.3 and would strand the registration; then the module built, loaded, registered and unloaded on a booted guest at 7.2.3-300.fc45 and at 7.3.0-0.rc0.260819gbd5f485f3f02, `/proc/filesystems` listing `hammer2` and the log carrying nothing but the unsigned-module taint. Style baseline 922 to 924, both hits the new guard |
| 0.2.108 | 2026-09-03 | the kernel of record was 7.2 while development had moved to 7.3, so the syntax gate and the style checker were both certifying against a kernel this port no longer targets, and the version guard written for 7.3 had only ever been measured against a merge-window snapshot rather than a released candidate (`485bdd8`, `1a52d29`, `7039fe8`) | `KERNEL_REF` and the checkpatch pin both at 7.3-rc1, measured against a `v7.3-rc1` tree built here: 46 syntax checks pass, the module links warning-clean, and the four `fs_bdev_*` declarations the guard assumes are present at the tag with unchanged signatures. The checker moved with no effect, 924 hits before and after. The tree's first `defconfig` build refused at the `static_assert`, naming `CONFIG_TRANSPARENT_HUGEPAGE`, which is that guard's first firing against a real kernel; reading `blkdev.h` at each tag puts the option's necessity at 6.16 through 7.3-rc1, the floor release being the one in the range that does not need it |
| 0.2.109 | 2026-09-04 | two CI runs failed on a change whose gates passed locally, because the gate selftests were never run: CI runs them as a step of its own, and `test-checkpatch.sh --selftest` carried `CHECKPATCH_REF=v7.2` as a literal, so once the baseline moved to v7.3-rc1 the version guard fired before the sha256 comparison the selftest exists to exercise. A hardcoded version inside the instrument whose whole purpose is to notice a recorded version disagreeing with a real one (`3bacd41`, `20bd44d`, `2b54560`) | the selftest reads the version out of the baseline's own first line, and `script/pre-push-check.sh` now runs every gate and every gate selftest before a push and refuses it on failure, enumerating the selftests by implementation as CI does. Both directions driven: a clean tree exits 0, one altered category count in the baseline names the moved line and exits 1. The first version treated COULD-NOT-RUN as a warning, which on this machine meant a real style regression would pass, since no local checker matches the baseline; it now fetches the pinned checker once and caches it by the sha256 the baseline records |
| 0.2.110 | 2026-09-04 | the syntax gate's green line named a release series that two different kernels satisfy, and the port had one tree for the claim it makes upstream and another the consuming distribution ships, with no way to tell from a run which had been used. The status file quoted a mainline measurement whose run had taken the store's `7.3.0-rc1-cachyos`, and separately still opened by denying the guest load recorded in 0.2.107 (`d6232bf`, `fa2f28e`, `2b8da5e`, `9b2a4cb`) | the release string and its patch provenance are read from the tree's own `EXTRAVERSION` and appear in the header line and the summary line, so a quotation carries the tree; the candidate search covers trees built here, extended by `H2_KERNEL_TREES`, and prefers an unpatched tree at the same version, making the default measure what the port claims. Both runs pass 46 checks with 0 failed, at `7.3.0-rc1` and at `7.3.0-rc1-cachyos`, and both are recorded, neither substituting for the other. The module links against each, `vermagic` differing, which is what makes them not interchange at load |
| 0.2.111 | 2026-09-04 | the distribution this port is developed alongside was renamed, and its old name sat in 54 places across 20 tracked files, in the vale style package the prose gate loads its rules from, and in six pointers into a specification directory that had already been renamed and so no longer resolved (`2c3df5f`) | substituted case-exact in three forms rather than by a case-insensitive expression, since a mixed spelling survives the careless sweep; the style package and the `.vale.ini` line naming it moved in the same commit, the two apart leaving the gate with no rules to load. Two lowercase occurrences are not the brand and were left: a host attribute another repository owns, and nothing else. Nothing under `src/` names the distribution, and in the vector files the name appears only in comments, never in the printf wording, exit status or constant spelling the consuming gate reads, which is the interface `0.2.11` froze against exactly this edit. The commit message counts 63 occurrences over 22 files, which is the working directory including two ignored files that commit does not touch, and the count that matters is the tracked one |
| 0.2.112 | 2026-09-04 | nothing this port has written had ever mounted anything, and the first mount found that `hammer2_mtx_upgrade_try()` was a predicate rather than an upgrade: it reported success only for a caller already holding the lock exclusively, so the loop in `hammer2_chain_unlock()` retried an upgrade that could never be granted and the first read of a file spun without end, unkillable, holding the mount until the guest was rebooted (`1f025fe`) | a `makefs` image mounts read-only at 7.2.3-300.fc45, `stat` on the root reports a directory at inode 1, `statfs` and `readdir` return the floors they are, and after the lock fix a read returns `EINVAL` at once with no `h2race2` line, no task in `D` state, and `umount` and `rmmod` both 0. The shared to exclusive transition releases the read side and takes the write side, restoring the caller's hold when it cannot, as the OpenBSD port does. The comment it replaces asserted every `_try` caller drops and re-acquires on failure, which that caller does not. Style baseline 924 to 925 |
| 0.2.113 | 2026-09-04 | no kernel measured carried `CONFIG_PROVE_LOCKING` or `CONFIG_DEBUG_KMEMLEAK`, so the lock order this port's shim imposes on a carried core was reasoned and never observed, and no prebuilt 7.3-rc1 carries either: Fedora Rawhide is still at the rc0 merge-window snapshot, Debian experimental tops out at 7.2 and openSUSE `Kernel:HEAD` publishes no binary (`c870ec4`) | a mainline `v7.3-rc1` kernel built here with both instruments, configured from the Artix guest's own `/proc/config.gz` and trimmed by `localmodconfig`, with both confirmed live before loading. Lockdep reports `possible recursive locking` on the first mount and names the cause, `missing lock nesting notation`: every chain lock takes its class from the one `init_rwsem()` in `hammer2_mtx_init()`, so a parent chain and its child are one class and lockdep then clears `debug_locks` and validates nothing further. Recorded as `DEFER(chain locks carry nesting notation)` with the bound the fix needs, `MAX_LOCKDEP_SUBCLASSES` being 8 against a deeper tree. kmemleak reports no unreferenced object across four scans. The run is also the first execution of the 7.3 device-open shim, against a virtio disk rather than a loop device |
| 0.2.114 | 2026-09-04 | every successful `->lookup` released one inode reference more than it held: `hammer2_inode_unlock()` drops as well as unlocks, in this port as in DragonFly, and the call was followed by a `hammer2_inode_drop()` besides. The carried comment above `hammer2_inode_get()` offers disposing of both via unlock plus drop, where both is the lock and the reference rather than two references. The mount root has a further reference through `pmp->iroot` and never reached zero, so the defect needed an operation that uses a looked-up inode before it could appear at all, and it appeared as a NULL dereference in `hammer2_xop_unset_ipdep()` on a subdirectory listing, with `ip->pmp` NULL and the cluster empty, which is the state the last drop leaves behind (`a33dff1`) | the drop removed and the reason written where the mistake was made. No sibling: the pattern appears once in the tree |
| 0.2.115 | 2026-09-04 | a mount could be entered and stat'd but not listed, `->iterate_shared` being the half of a usable directory that was not written (`e76ad21`) | upstream's `hammer2_vop_readdir()` with the cookie array dropped, that being a DragonFly NFS export interface whose values `ctx->pos` now carries, and the artificial entries dropped to `dir_emit_dots()` on the reasoning that removed the ISDOTDOT branch from `->lookup`. The position is a hash key rather than an index and trails by one entry, since the next key is unknown until the backend feeds it, so an emit that does not fit leaves the position on the entry that did not fit. Verified at `7.3.0-rc1` under lockdep and kmemleak on a `makefs` fixture: `find` returns all five paths, `umount` and `rmmod` return 0, kmemleak reports nothing, and reading a file still returns `EINVAL`. clang rejected `filename`, which is `unsigned char[]` on the media, against `dir_emit()`'s `const char *` where gcc did not, the second finding of that class the two-compiler rule has caught |
| 0.2.116 | 2026-09-04 | the first paragraph of `README.md` and the badge above it both said the port does not mount anything, which had been false since `1f025fe` and stayed false through three further pushes, because every correction went into the status document and nobody re-read the file a stranger reads first. The 0.4 row carried the same claim about `readdir`, the roadmap listed `getattr` among what the read-side row still owes, and the testing document gave the fixture attachment with `--persistent` when the disk under every recorded run is attached live only (`8882644`) | the README states what the mount can and cannot do and links the measurement; `getattr` recorded as not owed, with the mechanism read at `v7.3-rc1` rather than recalled, `vfs_getattr_nosec()` calling `generic_fillattr()` when `i_op->getattr` is NULL, so a Linux stat asks the inode where a BSD stat asks the filesystem; and the attachment's lifetime written down, live through a reset and gone after a shutdown, which is why a run of resets never revealed it. Found by auditing this session's own prose rather than by a gate: no gate reads a document for truth, and the count-shaped claim that named what the read-side row has left was written without checking one of its three terms |
| 0.2.117 | 2026-09-04 | `->iterate_shared` was recorded as verified on the strength of `find` and `ls`, and neither reaches the branch that matters: a 32 KiB `getdents64` buffer takes a five-path fixture in one call, so the emit that does not fit and the position left behind for the next call had never executed. A cookie that fails to advance shows up as a listing that repeats forever, which is the failure this operation is most likely to have and the one a passing `find` cannot rule out (`7508b12`) | `test/getdents-resume.c`, reading with a 64-byte buffer so the kernel stops mid-directory, and bounding its own call count so a position that does not advance fails the run rather than hanging the machine. The root gives 5 entries over 3 calls and the subdirectory 3 over 2, each name exactly once. It needs a booted guest holding a mount, so it is run by hand until the read-only fixture gate exists, and the inventory gate is what remembers it is there |
